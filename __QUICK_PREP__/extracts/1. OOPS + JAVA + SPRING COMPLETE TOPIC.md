# 1. Core Java

## 1.1 Language Fundamentals
### JVM, JRE, JDK
-	** What is the difference between JDK and JRE  ? ** 
-	** What is Java Virtual Machine (JVM)  ? ** 
-	** What are the different types of memory areas allocated by JVM  ? ** 
-	** How is the Java platform different from other platforms  ? ** 
-	** Why is Java called a 'write once and run anywhere' language  ? ** 
-	** Why is Java platform independent  ? ** 
-	** Is this valid in java ? can we instantiate interface in java? **
-	** Explain about Varargs in java? **
-	** Can we use Switch statement with Strings? **
-	** Explain about field hiding in java? **
-	** How can you determine if JVM is 32-bit or 64-bit from Java Program ? **
-	** How can we find the memory usage of JVM from Java code ? **
-	** How can you guarantee that the garbage collection takes place ? **
-	** When does JVM call the finalize() method ? **
-	** When would you use Serial Garabage collector or Throughput Garbage collector in Java ? **
-	** In Java, if you set an object reference to null, will the Garbage Collector immediately free the memory held by that object ? **
-	** How can you make an Object eligible for Garbage collection in Java ? **
-	** How can you determine if your program has a deadlock? JSP**
-	** How can we change the heap size of a JVM ? **
-	** Can you list Java Core Best Practices ? ** 
-	** can you explain JDK vs JVM  ? **
-	** Can you explain the JVM internal working ? **
-	** Can you explain Java memory management ? **
-	** Is Java Paid after Java 11 ? **
-	** Why no JRE from Java 9 ? **
-	** OpenJDK vs AdoptOpenJDK ? **
-	** What is the difference between HotSpot and OpenJDK OpenJ9 ? **
-	** What is the difference between stack memory and heap memory  ? **
-	** Permgen (why depricated after Java7) ? **
-	** What is Enterprise JavaBeans (EJB) ? **
-	** Can you explain public static void main(String args[]) in java ? **
-	** Is Java "pass-by-reference" or "pass-by-value" ? **
-	** What is the difference between Program Arguments and VM Arguments ? ** 
-	** Why main method in java is public static void ? **
-	** What is the use of String args[] in main method ? **
-	** Can we have multiple main method in java project / What happens if we run project with 2class having main method ? **
-	** Can we override / overload main method in java ? **
-	** Is main method compulsory in Java ? **

### Syntax, keywords, identifiers
-	** Which class contains clone method? Cloneable or Object class ? **
-	** Can we use String in the switch case statement in Java ? **
-	** What is a compile time constant in Java ? **
-	** How do you decide which type of Inner Class – Static or Non-Static to use in Java ? **
-	** What is the use of method references in Java ? **
-	** Do you think Java Enums are more powerful than integer constants ? **
-	** What are Java keywords ? ** 
-	** What are Java packages ? ** 
### Data types (primitive, reference)
-	** What is ASCII code  ? ** 
-	** What is Unicode  ? ** 
-	** What is the advantage of using Unicode characters in Java ? **
-	** What is the difference between character constant and string constant in Java  ? ** 
-	** What are constants and how to create constants in Java  ? ** 
-	** What does null mean in Java  ? ** 
-	** What is the right data type to represent Money (like Dollar/Pound) in Java ? **
-	** Out of an int and Integer, which one takes more memory ? **
-	** How can you convert an Array of bytes to String ? **
-	** You have a character array and a String.  Which one is more secure to store sensitive data (like password, date of birth, etc) ) ? **
-	** What is the difference between the expressions String s = "Temporary" and String s = new String("Temporary ")? Which one is better and more efficient ? **
-	** How can you check if a String is a number by using regular expression ? **
-	** How can we print an Array in Java ? **
-	** What is the difference between StringBuffer and StringBuilder ? **

### Type casting (implicit, explicit)
-	** What is type conversion in java? **
-	** Explain about Automatic type conversion in java? **
-	** Explain about narrowing conversion in java? **
-	** Explain about transient variables in java? **

### Operators (all categories)
-	** What is the difference between '>>' and '>>>' operators in Java  ? ** 
-	** Is there any difference between a = a + b and a += b expressions ? **
-	** What does the expression 1.0 / 0.0 return? Will there be any compilation error ? **
-	** Can you cast an int variable into a byte variable? What happens if the value of int is larger than byte ? **
-	** In Java, can we store a double value in a long variable without explicit casting ? **
-	** What will this return 5*0.1 == 0.5? true or false ? **
-	** What is the difference between x == y and x.equals(y) expressions in Java ? **
-	** What is the relation between x.hashCode() method and x.equals(y) method of Object class ? **
-	** Is ++ operation thread-safe in Java ? **
-	** Why Java does not support operator overloading ? **

### Control flow (if, switch, loops, break/continue)
### Arrays (1D, multi-D)

## 1.2 OOP
### General
-	** What is object-oriented programming and its features  ? ** 
-	** What is the difference between Functional Programming, OOPS, and AOP ? **
-	** What are the benefits of object-oriented programming language  ? ** 
-	** What is procedural programming language and its features  ? ** 
-	** What is the difference between object-oriented programming and object-based programming languages  ? ** 
-	** What are OOP concepts / the Four Pillars of OOP  ? ** 
-	** What are the main principles of object-oriented programming  ? ** 

### Packages
-	** What are packages in Java  ? ** 
-	** What is the reason to organize classes and interfaces in a package in Java ? **
-	** Is empty '.java' file name a valid source file name in Java ? **
-	** Can we have more than one package statement in a source file in Java  ? ** 
-	** Can we define a package statement after an import statement in Java  ? ** 
-	** What is the purpose of package in Java ? **
-	** What is java lang package ? **
-	** Which is the most important class in Java ? **
-	** Is it mandatory to import java lang package every time ? **
-	** Can you import same package or class twice in your class ? **
-	** What is a static import in Java ? **
-	** What is the difference between import static com test Fooclass and import com test Fooclass ? **
-	** Explain the importance of import keyword in java? **
-	** Explain about static imports in java? **
-	** Explain naming conventions for packages ? **
-	** What is classpath ? **
-	** What is jar ? **

### Identifiers
-	** What are identifiers in Java  ? ** 

### Access Modifiers
-	** What are access modifiers in Java  ? ** 
-	** What is the difference between access specifiers and access modifiers in Java  ? ** 
-	** What access modifiers can be used for class in Java  ? ** 
-	** What access modifiers can be used for methods in Java  ? ** 
-	** What access modifiers can be used for variables in Java  ? ** 
-	** What is the final access modifier in Java  ? ** 
-	** What is the use of "protected" access modifier  ? ** 
-	** What is the use of "default" access modifier in Java  ? ** 
-	** What are the four levels of access modifiers in Java  ? ** 
-	** What is the default access modifier in Java  ? ** 
-	** What are final and static modifiers for variables, methods, and classes in Java  ? ** 
-	** What are access modifiers in Java  ? ** 
-	** How do access modifiers relate to encapsulation in Java  ? ** 
-	** Can you change the access level of a method when overriding it in Java  ? ** 
-	** Why should instance variables almost always be private in Java  ? ** 
-	** When would you use a private constructor in Java  ? ** 
-	** Is protected access safe in Java  ? ** 
-	** What are access modifiers for methods and attributes in Java  ? ** 
-	** What access modifiers are allowed for top class in Java  ? ** 

### Classes, objects
-	** What is the superclass of every class in Java ? **
-	** How to create a custom singleton class in Java ? **
-	** How to implement a thread-safe singleton in Java ? **
-	** What is the difference between a class and an object in Java ? **
-	** What is class and object in Java OOP  ? ** 
-	** Where are classes and objects stored in memory in Java  ? ** 
-	** What is an anonymous object in Java  ? ** 
-	** What is an immutable class in Java  ? ** 
-	** Can we declare a class as private in Java  ? ** 
-	** What is the 'this' keyword in Java  ? ** 
-	** Why can't a static nested class access non-static members of the outer class  ? ** 
-	** How does a static member differ from an instance member in Java  ? ** 
-	** Why declare static variables as volatile in multi-threaded Java applications  ? ** 
-	** What happens to an object when it is no longer used in Java  ? ** 
-	** If you change a static variable in one object, does it change for all other objects of that class in Java  ? ** 
-	** How many objects can you create from a single class in Java  ? ** 
-	** What is a singleton class in Java  ? ** 
-	** What is the difference between an attribute and a method in Java OOP  ? ** 
-	** What are getter and setter methods in Java  ? ** 
-	** What is the difference between a parameter and an argument in Java methods  ? ** 
-	** What is a static method and when should you use it in Java  ? ** 
-	** Can a static method access instance attributes in Java  ? ** 
-	** What is pass-by-value vs pass-by-reference in Java  ? ** 
-	** What is the difference between pass by reference and pass by value ? **
-	** What are final attributes and methods in Java  ? ** 
-	** How do you prevent an attribute from being modified after the object is created in Java  ? ** 
-	** Why should you avoid making all attributes public in Java OOP  ? ** 
-	** If a method has a high number of parameters (e.g., 10+), how would you refactor it in Java OOP  ? ** 
-	** What are the different types of nested classes in Java  ? ** 
-	** Can we have multiple classes in a single file in Java  ? ** 
-	** Difference between object and reference? **
-	** Objects or references which of them gets garbage collected? **
-	** How many times finalize method will be invoked ? who invokes finalize() method in java? **
-	** Can we able to pass objects as an arguments in java? **
-	** When do we do encapsulation in Java? How to do encapsulation in Java? **
-	** What are reference variables in java? **
-	** Explain where variables are created in memory? **
-	** Explain the scope or life time of instance variables ? **
-	** Explain the scope or life time of class variables or static variables? **
-	** Explain scope or life time of local variables in java? **
-	** What is the difference between a class and an object in Java ? **
-	** Can we create an abstract class that extends another abstract class ? **
-	** What is the use of run time polymorphism in Java ? **
-	** Why do you use Upcasting or Downcasting in Java  ? **
-	** What is information hiding in Java ? **
-	** Why does Java provide default constructor ? **
-	** Why should you define a default constructor in Java ? **

### Inner Classes
-	** What is the impact of inner classes on filesystem after compilation in Java  ? ** 
-	** What is an anonymous inner class in Java  ? ** 
-	** What is the memory leak risk with inner classes in Java  ? ** 
-	** What is the difference between a static nested class and a non-static inner class in Java  ? ** 
-	** Can an inner class access private members of the outer class in Java  ? ** 
-	** What is an inner class and why would you use it in Java  ? ** 
-	** What are nested classes in java? **
-	** What is a Nested class? **
-	** How many types of Nested classes are in Java? **
-	** What is the difference between a Nested class and an Inner class in Java? **
-	** What are inner classes or non static nested classes in java? **
-	** Explain about member inner classes? **
-	** How to instantiate member inner class? **
-	** Explain about static nested classes in java? **
-	** How to instantiate static nested classes in java? **
-	** Why to use nested classes in java? (or)What is the purpose of nested class in java? **
-	** Why do we use Nested Classes? **
-	** Explain about method local inner classes or local inner classes in java? **
-	** Explain about features of local inner class? **
-	** Explain about anonymous inner classes in java? **
-	** Explain restrictions for using anonymous inner classes? **
-	** Can we access the non-final local variable, inside a Local Inner class? **

### Nested Interfaces
-	** What is a Nested interface? **
-	** Can an Interface be defined in a Class? **
-	** Do we have to explicitly mark a Nested Interface public static? **
-	** Why do we use Static Nested interface in Java? **

### Constructors
-	** Why do we need constructors in Java  ? ** 
-	** What is Constructor Overloading  ? ** 
-	** What is a constructor and why is it used in Java  ? ** 
-	** Can we have an immutable constructor in Java  ? ** 
-	** Can we have a static constructor in Java  ? ** 
-	** What is the super keyword in Java  ? ** 
-	** What is the super keyword used for calling constructors of abstract classes in Java  ? ** 
-	** How are constructors called during inheritance in Java  ? ** 
-	** How does a constructor differ from a method  ? ** 
-	** What is a default constructor  ? ** 
-	** Can a constructor be private  ? ** 
-	** Can you overload a constructor  ? ** 
-	** What is a copy constructor  ? ** 
-	** Does a constructor get inherited  ? ** 
-	** What is constructor chaining in Java  ? ** 
-	** Can a constructor be final, static, or abstract in Java  ? ** 
-	** What is the difference between this() and super() in Java  ? ** 
-	** Why is it a best practice to call super() as the first line in a constructor in Java  ? ** 
-	** What happens if you define a parameterized constructor but no default constructor in Java  ? ** 
-	** What value does a constructor return in Java  ? ** 
-	** Can we inherit a constructor in Java  ? ** 
-	** Why can't constructors be final, static, or abstract in Java  ? ** 
-	** How to call one constructor from another constructor in Java  ? ** 
-	** Will the compiler creates a default constructor if I have a parameterized constructor in the class? **
-	** Can we have a method name same as class name in java? **
-	** Can we override constructors in java? **
-	** Can Static methods access instance variables in java? **
-	** How do we access static members in java? **
-	** Can we override static methods in java? **
-	** How can you do constructor chaining in Java ? **
-	** Why do we use static initializers in Java ? **
-	** How can you access a non-static variable from the static context ? **
-	** What is constructor chaining in Java ? **
-	** What are the types of constructors in Java ? **
-	** What are initialization blocks in Java ? **
-	** What are static initialization blocks in Java ? **
-	** What are instance initialization blocks in Java ? **

### Method overloading/overriding
-	** What is the difference between method overloading and method overriding in Java  ? ** 
-	** What is method overriding in Java  ? ** 
-	** Can we override static methods and private methods  ? ** 
-	** Can we overload the main method in Java  ? ** 
-	** Why is overriding static and private methods not allowed in Java  ? ** 
-	** What is method overloading vs overriding in Java  ? ** 
-	** What are call by value and call by reference in Java  ? ** 
-	** What is the other name of Method Overloading ? **  
-	** How will you implement method overloading in Java ? **  
-	** What kinds of argument variations are allowed in Method Overloading ? **  
-	** Why it is not possible to do method overloading by changing return type of method in java ? **  
-	** Is it allowed to overload main() method in Java ? **  
-	** How do we implement method overriding in Java ? **  
-	** Are we allowed to override a static method in Java ? **  
-	** Why Java does not allow overriding a static method ? **  
-	** Is it allowed to override an overloaded method ? **  
-	** What is the difference between method overloading and method overriding in Java ? **  
-	** Does Java allow virtual functions ? **  
-	** What is meant by covariant return type in Java ? **
-	** Can we use multiple main methods in multiple classes ? **
-	** Can we use multiple main methods in same class ? **
-	** Does Java allow you to override a private or static method ? **
-	** Let say there is a method that throws NullPointerException in the superclass  Can we override it with a method that throws RuntimeException ? **
-	** When creating an abstract class, is it a good idea to call abstract methods inside its constructor ? **
-	** What are the rules of method overloading and method overriding in Java ? **
-	** Can you override an overloaded method in Java ? **

### Encapsulation
-	** What is Encapsulation  ? ** 
-	** how do you achieve Encapsulation  ? ** 
-	** Can a top-level class be private or protected in Java  ? ** 
-	** How to create an immutable class using only getter methods without setter methods in Java  ? ** 
-	** How do you create immutable objects in Java  ? ** 
-	** What is coupling in Java OOP (loose vs tight)  ? ** 
-	** What is cohesion in Java (java.io vs java.util example)  ? ** 
-	** What are final vs finally vs finalize in Java  ? ** 
-	** Why doesn't Java support pointers  ? ** 
-	** How do inner classes improve encapsulation in Java  ? ** 
-	** How does encapsulation differ from abstraction in Java  ? ** 


### Inheritance (types, diamond problem)
-	** How can you do multiple inheritances in Java ? **
-	** Can you create an Immutable object that contains a mutable object ? **
-	** What is Inheritance & why its IS-A relationship in Java  ? ** 
-	** What is the 'IS-A' relationship in Java  ? ** 
-	** What is the 'HAS-A' relationship in Java  ? ** 
-	** What is the difference between 'IS-A' and 'HAS-A' relationship in Java  ? ** 
-	** What is the importance of inheritance in Java  ? ** 
-	** How do you implement inheritance in Java  ? ** 
-	** How to access parent class properties and override default methods in Java  ? ** 
-	** What are the different types of inheritance in Java  ? ** 
-	** What is diamond problem in Java  ? ** 
-	** Does Java support multiple inheritance  ? ** 
-	** Can you inherit a final class or override a final method in Java  ? ** 
-	** Are private members inherited in Java  ? ** 
-	** What is method hiding in Java  ? ** 
-	** What is the instanceof operator in Java  ? ** 
-	** What is the purpose of 'this' keyword in java ? **
-	** Explain the concept of Inheritance ? **
-	** Which class in Java is superclass of every other class ? **
-	** Why Java does not support multiple inheritance ? **
-	** In OOPS, what is meant by composition ? **
-	** How aggregation and composition are different concepts ? **
-	** Why there are no pointers in Java ? **
-	** If there are no pointers in Java, then why do we get NullPointerException ? **
-	** What is the purpose of 'super' keyword in java ? **
-	** Is it possible to use this() and super() both in same constructor ? **
-	** What is the meaning of object cloning in Java ? **

### Polymorphism (compile/runtime)
-	** What is polymorphism in Java  ? ** 
-	** What is the use of run time polymorphism in Java ? **
-	** How does polymorphism promote extensibility in Java  ? ** 
-	** What are the types of polymorphism in Java  ? ** 
-	** What is runtime polymorphism in Java  ? **
-	** What is static vs runtime polymorphism in Java  ? ** 
-	** Is it possible to achieve runtime polymorphism by data members in Java  ? **
-	** Method overloading(static) / overriding(dynamic/runtime)  ? ** 
-	** What is static vs dynamic binding in Java  ? ** 
-	** Which OOP principle is associated with dynamic binding in Java  ? ** 
-	** Why is static binding faster than dynamic binding in Java  ? ** 
-	** How does the system handle dynamic binding internally in Java  ? ** 
-	** Why are static and final methods always statically bound in Java  ? ** 
-	** Can you override a static method in Java  ? ** 
-	** What are encapsulation, inheritance, and polymorphism in Java  ? ** 
-	** How does dynamic polymorphism allow multiple implementations of a service in Java  ? ** 
-	** What is the instanceof operator in Java  ? ** 
-	** Can you overload a method by changing only the return type in Java  ? ** 
-	** What is upcasting and downcasting in Java  ? ** 
-	** What is method hiding in the context of static binding in Java  ? ** 
-	** In Parent obj = new Child(); obj.someMethod(); if someMethod is overridden in Child, which binding is used in Java  ? ** 
-	** In Parent obj = new Child(); obj.staticMethod(); if staticMethod is static in both classes, which version runs in Java  ? ** 

### Abstraction (abstract class vs interface)
-	** [IMPORTANT] What is the difference between abstract class and interface in Java  ? ** 
-	** What are abstract classes in Java  ? ** 
-	** What is abstraction in Java  ? ** 
-	** How do you achieve abstraction in Java  ? ** 
-	** What are abstract methods in Java  ? ** 
-	** What is the difference between abstract class vs abstract method in Java  ? ** 
-	** What is the difference between abstract class and interface in Java  ? ** 
-	** What are interfaces in Java  ? ** 
-	** What are interface types marker and functional interfaces in Java  ? ** 
-	** How do you write custom marker interfaces in Java  ? ** 
-	** Can we create a constructor in an abstract class in Java  ? ** 
-	** Can abstract classes and interfaces have constructors in Java  ? ** 
-	** Can we instantiate an abstract class in Java  ? ** 
-	** What is the difference between abstract class vs interface in Java  ? ** 
-	** When should you use abstract class vs interface in Java  ? ** 
-	** Can an abstract class have a final method in Java  ? ** 
-	** Can a method be both abstract and final in Java  ? ** 
-	** What is an interface in Java and why is it used  ? ** 
-	** Can you instantiate an interface in Java  ? ** 
-	** What is the difference between an interface and an abstract class in Java  ? ** 
-	** What is a marker interface in Java  ? ** 
-	** Can an interface have method implementations in Java  ? ** 
-	** What happens if a class implements two interfaces with the same default method in Java  ? ** 
-	** Are all methods in an interface implicitly public in Java  ? ** 
-	** What does "design for an interface, not an implementation" mean in Java  ? ** 
-	** Why does a class implement an interface but extend a class in Java  ? ** 
-	** What is an abstract class in Java  ? ** 
-	** Can an abstract class have a constructor in Java  ? ** 
-	** Why can't we instantiate an abstract class in Java  ? ** 
-	** What is the difference between an abstract class and an interface in Java  ? ** 
-	** When would you choose an interface over an abstract class in Java  ? ** 
-	** Can an abstract class have zero abstract methods in Java  ? ** 
-	** Can a method be both abstract and static in Java  ? ** 
-	** Can an abstract class be final in Java  ? ** 
-	** What happens if a subclass does not implement all abstract methods of its parent in Java  ? ** 
-	** What is partial implementation in Java  ? ** 
-	** Can an abstract class implement an interface in Java  ? ** 
-	** How would you model a payment system using abstract classes in Java  ? ** 
-	** What is Abstraction in Object Oriented programming ? **
-	** How is Abstraction different from Encapsulation ? **
-	** What is an abstract class in Java ? **
-	** Is it allowed to mark a method abstract method without marking the class abstract ? **
-	** Is it allowed to mark a method abstract as well as final ? **
-	** Can we instantiate an abstract class in Java ? **
-	** What is an interface in Java ? **
-	** Is it allowed to mark an interface method as static ? **
-	** Why an Interface cannot be marked as final in Java ? **
-	** What is a marker interface ? **
-	** What can we use instead of Marker interface ? **
-	** How Annotations are better than Marker Interfaces ? **
-	** What is the difference between abstract class and interface in Java ? **
-	** Does Java allow us to use private and protected modifiers for variables in interfaces ? **
-	** How can we cast to an object reference to an interface reference ? **
-	** Define interface in java? **
-	** What is the purpose of interface? **
-	** Explain features of interfaces in java? **
-	** Can we define static methods inside interface? **
-	** Explain enumeration in java? **
-	** Explain restrictions on using enum? **
-	** Can we create an abstract class that extends another abstract class ? **
-	** Can you instantiate this class ? **

### Composition vs Aggregation
-	** What is association in OOP with respect to Java  ? ** 
-	** What are the two specialized forms of association in Java OOP  ? ** 
-	** How do you identify composition in Java code  ? ** 
-	** What is association in Java OOP (1-1, 1-M, M-M, M-1)  ? ** 
-	** What is aggregation (HAS-A relationship) in Java  ? ** 
-	** What is composition (strong relationship) in Java  ? ** 
-	** Inheritance vs composition: which is better in Java  ? ** 
-	** [IMPORTANT] What is inheritance vs composition and why prefer composition over inheritance  ? ** 
-	** How do you identify aggregation in Java code  ? ** 
-	** When should you use aggregation over composition in Java  ? ** 
-	** Give one example to understand aggregation vs composition in Java  ? ** 
-	** Why is the direction of association important in Java OOP  ? ** 
-	** Why is the death relationship important in Java OOP associations  ? ** 
-	** What is multiplicity in association in Java OOP  ? ** 
-	** How do you implement a many-to-many relationship in Java OOP  ? ** 
-	** If I delete a Car object, what happens to the Engine and the Driver in Java OOP composition  ? ** 
-	** Can a single relationship be both aggregation and composition in Java OOP  ? ** 
-	** How are composition and aggregation relationships drawn in UML diagrams  ? ** 

### final keyword & Final Classes/Methods & Blank Final Variables
-	** How can you change the value of a final variable in Java? **
-	** Can a class be marked final in Java? **
-	** How can we create a final method in Java? **
-	** How can we prohibit inheritance in Java? **
-	** Why Integer class in final in Java? **
-	** Is it allowed to declare main method as final? **
-	** What is a blank final variable in Java? **
-	** How can we initialize a blank final variable? **

### Static Variables
-	** In Java, why do we use static variable? **
-	** Why it is not a good practice to create static variables in Java? **

### Static Methods & Main
-	** What is the purpose of static method in Java? **
-	** Why do we mark main method as static in Java? **
-	** Is it possible to execute a program without defining a main() method? **
-	** What happens when static modifier is not mentioned in the signature of main method? **
-	** What is the difference between static method and instance method in Java? **
-	** What is the difference between static methods and static blocks in Java ? **

### Static Blocks
-	** In what scenario do we use a static block? **

### SOLID principles & GoF design pattern
### Strategy, State & Template Method
-	** When will you use Strategy Design Pattern in Java? **
-	** How Strategy design pattern is different from State design pattern in Java? **
-	** What is Template Method design pattern in Java? **
-	** What are the examples of Template method design pattern in JDK? **
-	** What are the examples of Strategy design pattern in JDK? **

### Observer, Command & Visitor
-	** What is Observer design pattern? **
-	** What are the examples of Observer design pattern in JDK? **
-	** What is the main difference between Observer and some other pattern? **  (implied by Observer‑related questions)
-	** What are the main uses of Command design pattern? **
-	** What are the examples of Command design pattern in JDK? **
-	** What are the examples of Visitor design pattern in JDK? **

### Singleton, Factory & Builder
-	** Have you used Singleton design pattern in your Java project? **
-	** What are the main uses of Singleton design pattern in Java project? **
-	** Why java.lang.Runtime is a Singleton in Java? **
-	** What is the way to implement a thread-safe Singleton design pattern in Java? **
-	** What are the examples of Singleton design pattern in JDK? **
-	** What are the examples of Factory Method design pattern implementation in Java? **
-	** What is the benefit we get by using static factory method to create object? **
-	** What are the examples of Abstract Factory design pattern in JDK? **
-   ** What is the difference between Factory and Abstract Factory design pattern? **
-	** What are the examples of Builder design pattern in JDK? **
-	** What is Builder design pattern? **
-	** What is the difference between Builder design pattern and Factory design pattern? **

### Decorator, Proxy, Adapter
-	** Can you explain Decorator design pattern with an example in Java? **
-	** What are the examples of Decorator design pattern in JDK? **
-	** How Decorator design pattern is different from Proxy pattern? **
-	** What are the examples of Proxy design pattern in JDK? **
-	** What are the different scenarios for using Proxy design pattern? **
-	** What is the main difference between Adapter and Proxy design pattern? **
-	** When will you use Adapter design pattern in Java? **
-	** What are the examples of Adapter design pattern in JDK? **

### Composite, Chain of Responsibility & Mediator
-	** What is a good scenario for using Composite design Pattern in Java? **
-	** What are the examples of Chain of Responsibility design pattern in JDK? **
-	** What are the examples of Mediator design pattern in JDK? **

### Interpreter, Memento, AntiPattern & Others
-	** What are the examples of Interpreter design pattern in JDK? **
-	** What is Memento design pattern? **
-	** What is an AntiPattern? **
-	** What is the design pattern suitable to access elements of a Collection? **
-	** What is the use of Interceptor design pattern? **
-	** What is the difference between Builder design pattern and Factory design pattern? **

### Producer‑Consumer, DI Injection & Object Extension
-	** How can we implement Producer Consumer design pattern in Java? **
-	** What design pattern is suitable to add new features to an existing object? **
-	** Which design pattern can be used when to decouple abstraction from the implementation? **
-	** Which is the design pattern used in Android applications? **
-	** How can we prevent users from creating more than one instance of singleton object by using clone() method? **
-	** What are the different scenarios to use Setter and Constructor based injection in Dependency Injection (DI) design pattern? **

### Principles, Patterns Categories & Architectural Patterns
-	** What is Open/closed design principle in Software engineering? **
-	** What is SOLID design principle? **
-	** What are the different categories of Design Patterns used in Object Oriented Design? **
-	** What are the Architectural patterns that you have used? **
-	** What are the popular uses of Façade design pattern? **
-	** What is a Data Access Object (DAO) design pattern? **

## 1.3 Java Memory Model (JMM)
### Methods
-	** Is 'main' a keyword in Java for the main method  ? ** 
-	** Can we write main method as public void static instead of public static void in Java  ? ** 
-	** What is the value of String array arguments in main method when no arguments are passed in Java  ? ** 
-	** What is the main() method in Java  ? ** 
-	** Why is the main() method public, static and void in Java  ? ** 

### Heap vs Stack
### Metaspace
### Object lifecycle
-	** Is the finalize method guaranteed to be called when System.gc() is invoked in Java  ? ** 
-	** How do you use the clone method to create a separate copy of an object in Java  ? ** 
-	** In java how do we copy objects? **
-	** How to copy Java objects (shallow copy vs deep copy vs copy constructor) ? **
-	** What is the other name of Shallow Copy in Java? **
-	** What is the difference between Shallow Copy and Deep Copy in Java? **
-	** What is a Singleton class? **
-	** What is the difference between Singleton class and Static class? **

### Escape analysis
### Happens-before
### Visibility, atomicity, ordering
-	** What are atomic operations in Java ? **
### Volatile keyword

## 1.4 Strings
-	** Why is String Immutable? **
-	** How do you create an Immutable Class in Java? **
-	** How do you create an immutable class with Java 14+ Records? **
-	** What are the limitations of Java Records? **
-	** How do you add custom methods and validation to a Java Record? **
-	** what is the difference between literal abc and new String(abc)? **
-	** What are the common String utility methods in Java ? **
-	** What is the difference between String, StringBuffer, and StringBuilder ? **
-	** What are the operations we can perform using String & StringBuilder ? **
-	** What is StringTokenizer in Java? **
-	** How do you use String.split() for tokenization? **
-	** Can you differentiate Split, Splice, Slice in java ? **
-	**What is the difference between equals() , == , and compareTo() in Java ? **

### Variables
-	** What is the default value of local variables in Java if not initialized  ? ** 
-	** What is the difference between byte and char data types in Java  ? ** 
-	** What is the default value of an object reference instance variable in Java  ? ** 

### Utils
-	** What is the difference between indexOf vs contains in Java  ? ** 
-	** What is the difference between length and length() method in Java  ? ** 
-	** What is the use of toString() method in java  ? **

### String immutability
-	** Why String class is Immutable or Final in Java ? **
-	** How will you make an Object Immutable in Java ? **
-	** Why an Object used as Key in HashMap should be Immutable ? **
-	** Why is String immutable in Java  ? ** 
-	** Why is the String class immutable in Java  ? ** 
-	** Why is it recommended to use char array instead of String for storing passwords in Java  ? ** 
-	** What is the meaning of Immutable in the context of String class in Java ? **
-	** Why a String object is considered immutable in java ? **
-	** How many objects does following code create ? **
-	** How many ways are there in Java to create a String object ? **
-	** How many objects does following code create ? **
-	** Why Java uses String literal concept ? **
-	** How will you create an immutable class in Java ? **

### String pool
-	** Why is String immutable in Java  ? ** 
-	** Will String s1 = "abc" and String s2 = new String("abc") create two different objects, and if yes, where  ? ** 
-	** Can String objects be created using both the new keyword and string literal in Java  ? ** 
### String vs StringBuilder vs StringBuffer
-	** String vs StringBuilder vs StringBuffer  ? ** 
-	** Why are StringBuffer and StringBuilder mutable while String is immutable in Java  ? ** 
-	** What is the basic difference between a String and StringBuffer object ? **
-	** Arrange the three classes String, StringBuffer and StringBuilder in the order of efficiency for String processing operations ? **
### Interning
-	** What is String interning ? **

## 1.5 Collections Framework
-	** Can you explain Collection Diagram ? **
-	** What is the difference between ArrayList and LinkedList ? **
-	** What is the difference between Set and List ? **
-	** What is the synchronized keyword and how does it relate to Concurrent Collections ? **
-	** What is the difference between HashMap and Hashtable (including internal working) ? **
-	** Is HashMap thread-safe ? **
-	** What is the difference between HashMap, HashSet, and HashTable ? **
-	** What is ConcurrentHashMap ? **
-	** What are the static methods present in the collections class ? **
-	** What are the differences between Java Streams and Collections ? **
-	** Collection vs Collections / What is the difference between the Java Collection framework and the Java Collections utility class ? **
-	** What is the difference between the hashCode() and equals() methods ? **
-	** How does HashMap internally work ? **
-	** What are the common Operators in Java ? **
-	** What is collections in Java  ? ** 
-	** What is the difference between Collections and Streams in Java  ? ** 
-	** The difference between AR list and linked list ? **
-	** What is collections framework ? **
-	** What is collection ? **
-	** Difference between collection, Collection and Collections in java? **
-	** Explain about Collection interface in java ? **
-	** List the interfaces which extends collection interface ? **
-	** What is the reason for overriding equals() method? **
-	** How does hashCode() method work in Java? **
-	** Is it ok to use random numbers in the implementation of hashcode() method in Java ? **
-	** Is it a good idea to use Generics in collections? **
-	** What is the purpose of Properties file? **
-	** What is the difference between Collection and Collections Framework in Java? **
-	** What are the main benefits of Collections Framework in Java? **
-	** What is the root interface of Collection hierarchy in Java? **
-	** What are the main differences between Collection and Collections? **
-	** What are the important points to remember while using Java Collections Framework? **
-	** Why Map interface does not extend Collection interface in Java? **
-	** Which methods do we need to override to use an object as key in a HashMap? **
-	** Let say there is a Customer class  We add objects of Customer class to an ArrayList  How can we sort the Customer objects in ArrayList by using customer firstName attribute of Customer class? **
-	** What are the benefits of using an unordered array over an ordered array ? **
-	** What is ConcurrentModificationException in Java ? **
-	** What are the differences between Enumeration, Iterator, and ListIterator in Java ? **

### List (ArrayList, LinkedList, Vector, Stack)
-	** How to remove duplicates from a List without using Set in Java  ? ** 
-	** How do you create a custom ArrayList that disallows duplicates in Java  ? ** 
-	** Explain List interface ? **
-	** Explain methods specific to List interface ? **
-	** List implementations of List Interface ? **
-	** Explain about ArrayList ? **
-	** Difference between Array and ArrayList ? **
-	** What is vector? **
-	** Difference between arraylist and vector ? **
-	** Define Linked List and its features with signature ? **
-	** Difference between arraylist and linkedlist? **
-	** Explain copyOnWriteArrayList and when do we use copyOnWriteArrayList? 43 **
-	** In which scenario, LinkedList is better than ArrayList in Java? **
-	** What are the differences between the two data structures: a Vector and an ArrayList? **
-	** What are the Java Collection classes that implement List interface? **
-	** What is the difference between Array and ArrayList in Java? **
-	** Difference between arraylist and linkedlist? **
-	** What are the differences between an ArrayList and a LinkedList data structure? **
-	** What is the difference between an ArrayList and a Vector data structure? **
-	** How will you convert an array of primitive integers int[] to a List collection? **
-	** How will you convert an array of String objects into a List? **
-	** How will you convert an array of integers like- int[] from a List? **
-	** How will you convert a List into an array of integers like- int[]? **
-	** How will you convert a List to a Set? **
-	** How will you remove duplicate elements from an ArrayList? **
-	** How will you efficiently remove elements while iterating a Collection? **
-	** How will you run a filter on a Collection? **
-	** How will you copy elements from a Source List to another list? **
-	** How will you reverse a List in Java? **
-	** How can we maintain a Collection with elements in Sorted order? **
-	** Between an Array and ArrayList, which one is the preferred collection for storing objects? **
-	** How will you create a shallow copy of a Map? **  (can be used in list‑like context too)
-	** How can we pass a Collection as an argument to a method and ensure that method will not be able to modify it? **
-	** What is the increasing order of performance for following collection classes in Java? **

### Set (HashSet, LinkedHashSet, TreeSet)
-	** Which one is faster, Set or HashMap in Java  ? ** 
-	** Explain about Sets ? **
-	** Implementations of Set interface ? **
-	** Explain HashSet and its features ? **
-	** Explain Tree Set and its features? **
-	** When do we use HashSet over TreeSet? **
-	** What is Linked HashSet and its features? **
-	** What are the differences between a List and Set collection in Java? **
-	** What are the differences between a HashSet and TreeSet collection in Java? **
-	** What are the Java Collection classes that implement Set interface? **
-	** What is the difference between a Set and a Map in Java? **
-	** What are the major differences between a HashSet and a HashMap? **
-	** What are the similarities between a HashSet and a HashMap in Java? **
-	** What is the difference between a HashSet and TreeSet collection in Java? **
-	** When do we use HashSet over TreeSet? **
-	** How is TreeSet class implemented in Java? **
-	** What is the advantage of NavigableMap over Map? **  (can include TreeSet‑like usage)
-	** How will you sort objects by Natural order in a Java List? **  (applies to TreeSet too)

### Map (HashMap, LinkedHashMap, TreeMap, Hashtable, ConcurrentHashMap)
-	** How is HashMap implemented internally in Java  ? ** 
-	** Can a HashMap have null keys, and how are they handled internally in Java  ? ** 
-	** How to check if a hashmap has this(any value) value  ? ** 
-	** hashmap can have multiple null keys  ? ** 
-	** hashmap can have multiple null values  ? ** 
-	** What is thread safety in HashMap in Java  ? ** 
-	** What is the difference between HashMap and HashSet in Java  ? ** 
-	** Why is ConcurrentHashMap used when parallel thread access and modification is required in Java  ? ** 
-	** Why does ConcurrentHashMap not allow null keys or values and throws a NullPointerException in Java  ? ** 
-	** How does ConcurrentHashMap use segment locking or bucket locking internally in Java  ? ** 
-	** Why is ConcurrentHashMap preferred over Hashtable due to its locking mechanism in Java  ? ** 
-	** What is TreeMap and how does it work internally in Java  ? ** 
-	** Explain about Map interface in java? **
-	** What is linked hashmap and its features? **
-	** What is SortedMap interface? **
-	** What is Hashtable and explain features of Hashtable? **
-	** Difference between HashMap and Hashtable? **
-	** What are the differences between a HashMap and a Hashtable in Java? **
-	** What are the differences between a HashMap and a TreeMap? **
-	** What are the differences between Collection and Collections in Java? **
-	** How will you insert, delete and retrieve elements from a HashMap collection in Java? **
-	** What is the default size of load factor in a HashMap collection in Java? **
-	** What is the significance of load factor in a HashMap in Java? **
-	** What is Hash Collision? How Java handles hash-collision in HashMap? **
-	** What are the Hash Collision resolution techniques? **
-	** What is the contract of hashCode() and equals() methods in Java? **
-	** What is the importance of hashCode() and equals() methods? **
-	** What is the difference between HashMap and ConcurrentHashMap in Java? **
-	** What are the main differences between HashMap and ConcurrentHashMap in Java? **
-	** Is it possible to replace Hashtable with ConcurrentHashMap in Java? **
-	** What is the main use of IdentityHashMap? **
-	** IdentityHashMap is different from a regular Map in Java? **
-	** How can we improve the performance of IdentityHashMap? **
-	** Is IdentityHashMap thread-safe? **
-	** What is a WeakHashMap in Java? **
-	** How can we create a Map with reverse view and lookup in Java? **
-	** How can we create an empty Map in Java? **
-	** What is a NavigableMap in Java? **
-	** What is the difference between headMap(), tailMap() and subMap() methods of NavigableMap? **
-	** What is the difference between descendingKeySet() and descendingMap() methods of NavigableMap? **
-	** Can we get a Map from a Stream in Java? **
-	** Can you explain how HashMap works in Java? **
-	** Can you explain how HashSet is implemented in Java? **
-	** Which two methods should be always implemented by HashMap key Object ? **

### Queue (PriorityQueue, Deque)
-	** What is the difference between Queue and Stack data structures? **
-	** What is the difference between peek(), poll() and remove() methods of Queue interface in java? **
-	** What is a PriorityQueue in Java? **
-	** How will you create a Map from a Stream in Java? **  (can be used with queues)
-	** What are the popular implementations of Deque in Java? **
-	** What is BlockingQueue in Java Collections? **

### Iterators vs ListIterator
-	** Define Iterator and methods in Iterator? **
-	** In which order the Iterator iterates over collection? **
-	** Explain ListIterator and methods in ListIterator? **
-	** Explain about fail fast iterators in java? **
-	** Explain about fail safe iterators in java? **
-	** What are the differences between fail-fast and fail-safe iterators? **
-	** What is an Iterator in Java? **
-	** What is the difference between Iterator and Enumeration in Java? **
-	** What is the difference between an Iterator and ListIterator in Java? **
-	** What is the design pattern used in the implementation of Enumeration in Java? **
-	** What are the different ways to iterate elements of a list in Java? **
-	** Why ListIterator has add() method but Iterator does not have? **
-	** What is the difference between remove() method of Collection and remove() method of Iterator? **
-	** What is the difference between Iterator and Enumeration? **
-	** What happens when you put a key object in a HashMap that is already present ? **
-	** Explain the difference between fail-fast and fail-safe iterators ? **
-	** What is the difference between poll() and remove() methods of Queue in Java ? **
-	** What are the situations in which you choose HashSet or TreeSet ? **
-	** Between Enumeration and Iterator, which one has better performance in Java ? **
-	** What are the different ways to sort a collection in Java ? **
-	** Why Collection interface doesn't extend Cloneable and Serializable interfaces ? **
-	** Between HashSet and TreeSet collections in Java, which one is better ? **

### Comparable vs Comparator
-	** How do you sort objects based on ID and name using Comparable and Comparator in Java  ? ** 
-	** How do you create Comparators in Java for sorting objects based on different attributes  ? ** 
-	** How do you explain custom sorting using Comparator in Java  ? ** 
-	** How does the compareTo() method compare two objects and what values does it return in Java  ? ** 
-	** What are Comparable vs Comparator in Java  ? ** 
-	** Difference between Comparator and Comparable in java? **
-	** What are the differences between Comparable and Comparator? **
-	** What is the reason for overriding equals() method? **
-	** What is the contract of hashCode() and equals() methods in Java? **
-	** Why Map interface does not extend Collection interface in Java? **  (context‑related)
-	** When do we use a natural ordering vs Comparator? **  (implied by sorting questions)

### Internal working (hashing, resizing, load factor)
-	** What are all the methods available in HashMap in Java  ? ** 
-	** What is the default capacity of List, Map, and Set in Java  ? ** 
-	** Why is proper implementation of equals() and hashCode() methods necessary to avoid duplicates in collections  ? ** 
-	** Why does a Set implementation allow duplicate objects if equals and hashCode methods are not overridden for custom objects in Java  ? ** 
-	** How does the contract between equals and hashCode methods determine if duplicate objects are allowed in Java  ? ** 
-	** What is hashing collision and how is it handled in Java  ? ** 
-	** How does HashMap internally use linked list nodes in Java  ? ** 
-	** What is the default size of load factor in a HashMap collection in Java? **
-	** What is the significance of load factor in a HashMap in Java? **
-	** What is Hash Collision? How Java handles hash-collision in HashMap? **
-	** What are the Hash Collision resolution techniques? **
-	** What is the importance of hashCode() and equals() methods? **
-	** What is the contract of hashCode() and equals() methods in Java? **
-	** Can you explain how HashMap works in Java? **
-	** Can you explain how HashSet is implemented in Java? **
-	** How remove() method is implemented in a HashMap? **
-	** How is TreeMap class implemented in Java? **
-	** What is the increasing order of performance for following collection classes in Java? **

### Fail-fast vs fail-safe
-	** What is the difference between fail-fast and fail-safe iterators in Java  ? ** 
-	** Explain about fail fast iterators in java? **
-	** Explain about fail safe iterators in java? **
-	** What are the differences between fail-fast and fail-safe iterators? **
-	** What is the difference between Fail-fast and Fail-safe iterator in Java? **
-	** Why do we sometime get ConcurrentModificationException during iteration? **
-	** CopyOnWriteArrayList class is different from ArrayList and Vector classes? **
-	** What is CopyOnWriteArrayList? How it is different from ArrayList in Java? **
-	** How does ConcurrentHashMap work in Java? **

### Scenario based
-	** Write java code to find missing values from an array  ? ** 
-	** In Java, how will you decide when to use a List, Set or a Map collection? **
-	** In which scenario, LinkedList is better than ArrayList in Java? **
-	** How will you convert a Collection to SynchronizedCollection in Java? **
-	** How can we synchronize the elements of a List, a Set or a Map? **
-	** Since when is UnsupportedOperationException thrown in Java  (When unmodifiable/views are modified) ? **
-	** What is the difference between Synchronized Collection and Concurrent Collection? **
-	** What is the scenario to use ConcurrentHashMap in Java? **
-	** How will you create a Map with reverse view and lookup in Java? **
-	** How can we create a Map with reverse view and lookup in Java? **
-	** When is UnsupportedOperationException thrown in Java? **
-	** Between an Array and ArrayList, which one is the preferred collection for storing objects? **
-	** What are the main Concurrent Collection classes in Java? **
-	** How will you convert a Map to a List in Java? **
-	** Let say there is a Customer class  We add objects of Customer class to an ArrayList  How can we sort the Customer objects in ArrayList by using customer firstName attribute of Customer class? **
-	** How can we get a Stream from a List in Java? **
-	** Can we get a Map from a Stream in Java? **

## 1.6 Generics
### Wrapper classes
-	** Explain wrapper classes in java & What are they ? **
-	** Explain different types of wrapper classes in java? **
-	** What are wrapper classes in Java ? **
-	** What are data type conversions and fundamentals in Java ? **
-	** What is variable shadowing in Java ? **

### Type safety
-	** How does using generics with lists help in avoiding type cast issues in Java  ? ** 
### Wildcards (?, extends, super)
### Type erasure
### Generic methods/classes

## 1.7 Exception Handling
-	** What is Exception Handling in Java ? **
-	** What is Exception handling in java ? **
-	** What is an exception in java ? **
-	** State some situations where exceptions may arise in java ? **
-	** What are advantages of Exception handling in java ? **
-	** In how many ways we can do exception handling in java ? **
-	** List out five keywords related to Exception handling  ? **
-	** Can we write any code after throw statement ? **
-	** Can we re-throw an Exception in Java ? **
-	** What is the difference between throw and throws in Java ? **
-	** Explain throw keyword in java ? **
-	** Explain importance of throws keyword in java ? **
-	** When do you use Exception or Error in Java? What is the difference between these two ? **
-	** In Java, what is the difference between throw and throws keywords ? **
-	** What happens to the Exception object after the exception handling is done ? **
-	** How is ClassNotFoundException thrown ? **
-	** What is the difference between Fail -	fast and Fail -	safe ? **
-	** What is the difference between Error and Exception ? **
-	** Can you explain Error and Exception Diagram ? **
-	** Can you explain Types of Java Exceptions & Error ? ** 
-	** How do you handle multiple exception types in a single catch block ? **
-	** What design pattern is used to implement exception handling ? **
-	** What is the difference between throw and throws ? **
-	** What happens if you catch a general Exception before a specific Checked Exception ? **
-	** How do you create and handle a Java Custom Exception ? **
-	** How do you handle custom exceptions in Spring Boot ? **
-	** What are the rules for Exception Handling in Method Overriding ? **

### Checked vs unchecked
-	** What is checked and unchecked exceptions in Java  ? ** 
-	** Can compile time exceptions be handled at compile time in Java  ? ** 
-	** Is FileNotFoundException a checked exception or a compile time exception in Java  ? ** 
-	** Why is understanding the order of exception hierarchy important while handling exceptions in Java  ? ** 
-	** In Java, what are the differences between a Checked and Unchecked ? **
-	** What is the base class for Error and Exception classes in Java ? **
-	** What is an eror in Java ? **
-	** What are checked Exceptions ? **
-	** What are unchecked exceptions in java ? **
-	** Explain differences between checked and Unchecked exceptions in java? 20 **
-	** What is default Exception handling in java ? **
-	** Can we use catch statement for checked exceptions ? **

### try-catch-finally
-	** Can the finally block be stopped from executing in Java  ? ** 
-	** What is a finally block in Java ? **
-	** What is the use of finally block in Java ? **
-	** Can we create a finally block without creating a catch block ? **
-	** Do we have to always put a catch block after a try block ? **
-	** In what scenarios, a finally block will not be executed ? **
-	** Explain try and catch keywords in java ? **
-	** Can we have try block without catch block ? **
-	** Can we have multiple catch block for a try block ? **
-	** Explain importance of finally block in java ? **
-	** Can we have any code between try and catch blocks ? **
-	** Can we have any code between try and finally blocks ? **
-	** Can we catch more than one exception in single catch block ? **
-	** Explain the importance of finally over return statement ? **
-	** Explain a situation where finally block will not be executed ? **

### try-with-resources
### Custom exceptions
-	** How to create a custom exception in Java  ? ** 
-	** What are user defined exceptions ? **
-	** When we override a method in a Child class, can we throw an additional Exception that is not thrown by the Parent class method ? **
-	** Can we rethrow the same exception from catch handler ? **
-	** Can we nested try statements in java ? **

### Exception propagation
-	** How to delegate exception handling to the next method caller in Java  ? ** 
-	** What is the concept of Exception Propagation ? **
-	** Explain the importance of throwable class and its methods ? **
-	** When ClassNotFoundException will be raised  ? **
-	** When NoClassDefFoundError will be raised  ? **

## 1.8 Multithreading & Concurrency
-	** How can you make sure that N threads can access N resources without deadlock ? **
-	** What is a thread local variable in Java ? **
-	** What is the difference between sleep() and wait() methods in Java ? **
-	** What is difference between CyclicBarrier and CountDownLatch class ? **
-	** How will you take thread dump in Java ? **
-	** Can you catch an exception thrown by another thread in Java ? **
-	** Why do you use volatile keyword in Java ? **
-	** How can you mark an array volatile in Java ? **
-	** What is the difference between a process and a thread in Java ? **
-	** How can we share an object between multiple threads ? **
-	** What is process ? **
-	** What is thread in java? **
-	** What is a Thread in Java? **
-	** Difference between process and thread? **
-	** What is multitasking ? **
-	** What are different types of multitasking? **
-	** What are the benefits of multithreaded programming? **
-	** What are the advantages of Multithreading? **
-	** What are the disadvantages of Multithreading? **
-	** How does Multithreading works in Java? **
-	** What is a Thread in Java? **
-	** What is the priority of a Thread and how it is used in scheduling? **
-	** What is the default priority of a thread in Java? **
-	** What are the three different priorities that can be set on a Thread in Java? **
-	** What are the different states of a Thread in Java? **
-	** What is a Scheduler? **
-	** What is the main difference between process and thread? **
-	** What is a process and thread in the context of Java? **
-	** What is the minimum number of Threads in a Java program? **
-	** What are the properties of a Java thread? **
-	** How will you set the priority of a thread in Java? **
-	** What is the purpose of Thread Groups in Java? **
-	** How do you access the current thread in a Java program? **
-	** What is the volatile keyword ? **
-	** What is the difference between a process and a thread ? **
-	** What are the basics of threads in Java ? **
-	** What are the different ways to create and use threads in Java ? **
-	** What is Garbage Collection in Java ? **
-	** What is the difference between p* (Percentile) latencies ? **
-	** What is the difference between Multithreading and Concurrency ? **
-	** What is the difference between Multitasking and Multiprocessing ? **
-	** What is the difference between Multithreading and Multiprocessing ? **	
-	** What is Deadlock in Java ? **
-	** What is the life cycle of a thread in Java ? **
-	** What is the join() method in Java threads ? **
-	** How do you name a thread in Java ? **
-	** How do you name threads when using an ExecutorService with a ThreadFactory ? **
-	** What is a Daemon Thread in Java ? **
-	** What is a Thread Pool in Java ? **
-	** Can you provide a code example for a FixedThreadPool ? **
-	** How do you use Future to get results from a Thread Pool ? **
-	** How does CompletableFuture improve on the Future interface ? **
-	** How do you use a custom Executor with CompletableFuture ? **
-	** What is the difference between handle() and exceptionally() in CompletableFuture ? **
-	** What is the difference between CompletableFuture.allOf() and anyOf() ? **
-	** How do you handle partial failures with CompletableFuture.allOf() ? **
-	** What is a Synchronized Block in Java ? **
-	** What is the difference between a Synchronized block and ReentrantLock ? **
-	** When would you NOT use synchronized ? **
-	** What will happen when we call start() two times on the same thread ? **
-	** What is Callable in Java threads ? **
-	** What is the need for threads in Java ? **
-	** How do you create a thread in Java ? **
-	** How do you create a thread by extending the Thread class ? **
-	** How do you create a thread by implementing the Runnable interface ? **
-	** How do you run a thread in Java ? **
-	** What are the different states of a thread ? **
-	** What is the priority of a thread and how do you change it ? **
-	** What is ExecutorService ? **
-	** What are the different ways of creating Executor Services ? **
-	** Can you provide a code example for ExecutorService ? **
-	** How do you check whether an ExecutorService task executed successfully ? **
-	** Can you provide an example of a synchronized block ? **
-	** Can a static method be synchronized ? **
-	** What are some other important methods in the Thread class ? **
-	** What are the important methods in Java for inter-thread communication ? **
-	** Can you provide a code example using wait() and notify() ? **
-	** Why is notifyAll() generally preferred over notify() ? **

### Creating, Starting & Stopping / Managing Threads
-	** Explain thread in java? **
-	** List Java API that supports threads? **
-	** Explain about main thread in java? **
-	** In how many ways we can create threads in java? **
-	** Explain creating threads by implementing Runnable class? **
-	** Explain creating threads by extending Thread class ? **
-	** Which is the best approach for creating thread ? **
-	** Is it possible to call run() method instead of start() on a thread in Java? **
-	** Can we start a thread two times in Java? **
-	** Can we restart a dead thread in java? **
-	** Can we restart a thread already started in java? **
-	** What happens if we don't override run method ? **
-	** Can we overload run() method in java? **
-	** How will you create a Thread in Java? **
-	** Is it possible to call run() method instead of start() on a thread in Java? **
-	** Can we start a thread two times in Java? **
-	** How can we stop a thread in the middle of execution in Java? **
-	** Why we should not stop a thread by calling its stop() method? **
-	** What is a Shutdown hook in Java? **
-	** How can we make sure that Parent thread waits for termination of Child thread? **
-	** What is the purpose of join() method in Thread class? **
-	** There are two threads T1 and T2? How will you ensure that these threads run in sequence T1, T2 in Java? **

### Thread Scheduling, Lifecycle & Control
-	** What is the difference between a thread and a process  ? ** 
-	** Explain the importance of thread scheduler in java? **
-	** Explain the life cycle of thread? **
-	** What are thread priorities and importance of thread priorities in java? **
-	** Explain different types of thread priorities ? **
-	** How to change the priority of thread or how to set priority of thread? **
-	** If two threads have same priority which thread will be executed first ? **
-	** What is a Thread's priority and how it is used in scheduling? **
-	** What are the differences between Pre-emptive Scheduling Scheduler and Time Slicing Scheduler? **
-	** What all methods are used to prevent thread execution ? **
-	** Explain yield() method in thread class ? **
-	** Is it possible for yielded thread to get chance for its execution again ? **
-	** Explain the importance of join() method in thread class? **
-	** Explain purpose of sleep() method in java? **
-	** Assume a thread has lock on it, calling sleep() method on that thread will release the lock? **
-	** Can sleep() method causes another thread to sleep? **
-	** Explain about interrupt() method of thread class ? **
-	** In what scenarios can we interrupt a thread? **

### Runnable vs Callable
### Executor framework
-	** What is the relation between Executor and ExecutorService interface? **
-	** What will happen on calling submit() method of an ExecutorService instance whose queue is already full? **
-	** What is a ScheduledExecutorService? **
### Thread pools
-	** How will you create a Thread pool in Java? **
-	** What is the main difference between Runnable and Callable interface? **
-	** What are the uses of Future interface in Java? **
-	** In Java 8, can we process stream operations with a Thread pool? **
-	** What are the scenarios to use parallel stream in Java 8? **
### Synchronization
-	** What is synchronization in Java  ? ** 
-	** What is a synchronized method in Java  ? ** 
-	** What are synchronized methods ? **
-	** When do we use synchronized methods in java? **
-	** When a thread is executing synchronized methods , then is it possible to execute other synchronized methods simultaneously by other threads? **
-	** When a thread is executing a synchronized method , then is it possible for the same thread to access other synchronized methods of an object ? **
-	** What are synchronized blocks in java? **
-	** When do we use synchronized blocks and advantages of using synchronized blocks? **
-	** Can we synchronize static methods in java? **
-	** Can we use synchronized block for primitives? **
-	** What is the purpose of Synchronized block in Java? **
-	** Can we mark a constructor as synchronized in Java? **
-	** Which intrinsic lock is acquired by a synchronized method in Java? **
-	** How can a synchronized block cause Thread starvation in Java? **

### Locks (ReentrantLock, ReadWriteLock)
-	** Can one thread block the other thread? **
-	** What is a lock or purpose of locks in java? **
-	** In how many ways we can do synchronization in java? **
-	** What is class level lock ? **
-	** Is it possible to lock an object for exclusive use by a thread? **
-	** Explain which of the following methods releases the lock when yield(), join(),sleep(),wait(),notify(), notifyAll() methods are executed? **
-	** What is synchronization in Java? **
-	** What is static synchronization? **
-	** Can we use primitive values for intrinsic locks? **
-	** Do we have re-entrant property in intrinsic locks? **
-	** What is an atomic operation? **
-	** Can we consider the statement i++ as an atomic operation in Java? **
-	** What are the Atomic operations in Java? **
-	** Can you check if following code is thread-safe? **
-	** Can you check if following code is thread-safe for retrieving an integer value from a Queue? **
-	** How can we check if a thread has a monitor lock on a given object? **
-	** What is a Deadlock situation? **
-	** What are the minimum requirements for a Deadlock situation in a program? **
-	** How can we prevent a Deadlock? **
-	** How can we detect a Deadlock situation? **
-	** What is a Livelock? **
-	** What is Thread starvation? **
-	** What is a Race condition? **
-	** Can we use a primitive values for intrinsic locks? **
-	** How will you improve the performance of an application by multithreading? **
-	** What is scalability in a Software program? **
-	** How will you calculate the maximum speed up of an application by using multiple processors? **
-	** What is Lock contention in multi-threading? **
-	** What are the techniques to reduce Lock contention? **
-	** What technique can be used in following code to reduce Lock contention? **
-	** What is Lock splitting technique? **
-	** Which technique is used in ReadWriteLock class for reducing Lock contention? **
-	** What is Lock striping? **
-	** What is a CAS operation? **
-	** Which Java classes use CAS operation? **
-	** Is it always possible to improve performance by object pooling in a multi-threading application? **
-	** How can techniques used for performance improvement in a single thread application may degrade the performance in a multi-threading application? **

### Volatile
### Atomic classes
### Concurrent collections
-	** What is the difference in concurrency in HashMap and in Hashtable? **
-	** How will you create synchronized instance of List or Map Collection? **
-	** What is a Semaphore in Java? **
-	** What is a CountDownLatch in Java? **
-	** What is the difference between CountDownLatch and CyclicBarrier? **

### ForkJoinPool
-	** What are the scenarios suitable for using Fork/Join framework? **
-	** What is the difference between RecursiveTask and RecursiveAction class? **
-	** How Stack and Heap work in Java multi-threading environment? **
-	** How can we take Thread dump in Java? **
-	** Which parameter can be used to control stack size of a thread in Java? **

### CompletableFuture
### Deadlock, livelock, starvation
### Producer-consumer, Inter‑Thread Communication & Monitor Methods
-	** Explain about interthread communication and how it takes place in java?  28 **
-	** Explain wait(), notify() and notifyAll() methods of object class ? **
-	** Explain why wait() , notify() and notifyAll() methods are in Object class rather than in thread class? **
-	** Explain IllegalMonitorStateException and when it will be thrown? **
-	** when wait(), notify(), notifyAll() methods are called does it releases the lock or holds the acquired lock? **
-	** How notify() method is different from notifyAll() method? **
-	** What is an important point to consider while passing an object from one thread to another thread? **
-	** Which two methods of Object class can be used to implement a Producer Consumer scenario? **
-	** JVM determines which thread should wake up on notify()? **
-	** What is a Fair lock in multi-threading? **

## Daemon Threads, Yield, Busy Waiting, Thread Groups & Thread Locals
-	** What are thread groups? **
-	** What are thread local variables ? **
-	** What are daemon threads in java? **
-	** How to make a non daemon thread as daemon? **
-	** Can we make main() thread as daemon? **
-	** How will you make a user thread into daemon thread if it has already started? **
-	** What is a daemon thread in Java? **
-	** How can we make a regular thread Daemon thread in Java? **
-	** How will you make a user thread into daemon thread if it has already started? **
-	** What is the use of yield() method in Thread class? **
-	** What is Busy waiting in Multi-threading? **
-	** How can we prevent busy waiting in Java? **
-	** Can we use Thread sleep() method for real-time processing in Java? **
-	** Can we wake up a thread that has been put to sleep by using Thread sleep() method? **
-	** What are the two ways to check if a Thread has been interrupted? **
-	** How will you handle InterruptedException in Java? **
-	** What are the rules for creating Immutable Objects? **
-	** What is the use of ThreadLocal class? **
-	** What are the scenarios suitable for using ThreadLocal class? **

## Multithreading Scenario based 
-	** How do you implement thread-safe queues in Java ? **
-	** How do you implement thread-safe HashMaps in Java ? **
-	** How do you execute tasks based on dependencies in Java ? **
-	** Can you provide a code example for a 3-step dependency chain using CompletableFuture ? **
-	** How do you combine two independent tasks using thenCombine() ? **
-	** How do you manage shared resources in Java ? **
-	** Can you provide a code example for a shared counter using AtomicInteger ? **
-	** How do you prevent deadlocks in Java ? **
-	** Can you provide a code example of how Lock Ordering prevents deadlocks ? **
-	** How do you implement a Task Scheduler in Java ? **
-	** How do you execute dependent tasks in parallel ? **
-	** Can you explain the dining philosophers problem ? ** 

## 1.9 I/O & NIO
-	** What is the purpose of native method in Java? **
-	** What is System class? **
-	** What is System, out and println in System out println method call? **
-	** Can you explain Input / Output streams Diagram ? **
-	** What are the common classes in Java I/O ? **
-	** Can you provide examples for all the common Java I/O classes ? **
-	** How do you use BufferedReader and BufferedWriter for faster text processing ? **
-	** How do you use OutputStreamWriter to specify a specific character encoding ? **
-	** Large File Uploads: You need to handle a multi-gigabyte file upload on a server. Would you load the entire file into a byte array? Why is using an InputStream to read in chunks (e.g., 8192 bytes) a better approach for memory management ? **
-	** Performance Optimization: Your application is performing slow disk writes. How would you use a superstructure class like BufferedOutputStream or BufferedWriter to improve the speed of these I/O operations ? **
-	** Filtering Files: You are given a directory and need to list only the .txt files. How would you implement the FilenameFilter or FileFilter interface to achieve this ? **
-	** Random Access Requirement: Suppose you have a large data file and need to jump directly to the 500th byte to update a specific value without reading the entire file. Which class would you use, and why is it unique compared to standard streams ? **
-	** Character Encoding Challenges: You are reading a file encoded in UTF-8, but your default system encoding is different. How do you use InputStreamReader as a bridge to ensure characters are not corrupted during the read ? **
-	** Primitive Data Extraction: You have a binary file containing a series of int and double values. Which specialized class would you use to read these primitives directly instead of manually parsing bytes ? **
-	** Log File Analysis: You need to read a log file and process it line-by-line. Why is BufferedReader.readLine() preferred over reading individual bytes, and what value does it return when it reaches the end of the file ? **
-	** Object Persistence: You need to save the state of a user's session object to a local disk so it can be restored after a restart. Explain the process of Serialization and which interface the object must implement ? **
-	** Securing Sensitive Data: During serialization of a User object, you want to ensure the password field is not saved to the disk. Which keyword would you use to exclude this field ? **
-	** Custom Serialization: A standard serialization mechanism is not sufficient for your complex object because you need to encrypt certain fields before they are written. How would you use the Externalizable interface and its writeExternal() method to gain this control ? **
-	** Network Transfer: You are sending objects over a socket between two different Java applications. Why must these objects be serialized, and what happens if one of the included object references is not serializable ? **
-	** Thread Blocking: In a high-concurrency environment, one of your threads is stuck waiting for a file read. Explain why threads block on I/O and what state they enter while waiting ? **
-	** Modern I/O (NIO): Your application needs to handle thousands of simultaneous network connections (like a chat server). Why might you choose Java NIO (Non-blocking I/O) over traditional java.io streams ? **
-	**Memory Management**: You need to process a 10GB log file on a machine with only 2GB of RAM. How do you read this file without causing an `OutOfMemoryError` ? **
-	**Data Integrity**: You are transferring a `User` object over a network, but the `password` field must not be sent. How do you ensure this specific field is skipped during the serialization process ? **
-	**Performance Bottleneck**: A legacy application is writing data to a disk byte-by-byte, causing high CPU usage and slow execution. Which wrapper class would you implement to minimize the number of system calls ? **
-	**Random Access**: You are developing a database-like application where you need to jump to the 1024th byte of a file, update a record, and jump back to the start. Which class allows this "seeking" behavior ? **
-	**Encoding Issues**: A file generated on a Linux server (UTF-8) is being read incorrectly on a Windows machine (UTF-16). How do you use `InputStreamReader` to explicitly handle the character encoding ? **
-	**Persistence Versioning**: You serialized an object to a file yesterday. Today, you added a new field to the class. Why might the application throw an `InvalidClassException` when trying to read the old file, and how do you prevent this ? **
-	**Primitive Extraction**: You have a binary file containing a sequence of `long` and `double` values. Instead of manual bit-shifting, which class provides methods like `readLong()` to extract these values directly ? **
-	**Directory Filtering**: You need to write a utility that deletes all files older than 30 days within a nested folder structure. How would you use `FileFilter` or `Files.walk()` to identify these specific files ? **
-	**Bridge Pattern**: You have an existing method that accepts a `Reader`, but your source data is a `FileInputStream`. How do you bridge the gap between these two incompatible types ? **
-	**Custom Serialization**: The default Java serialization is too slow for your high-frequency trading app. How would you implement `Externalizable` to manually control exactly how bytes are written and read ? **
-	**Resource Leaks**: You have a method that opens multiple streams. If an exception occurs on the second stream, the first one remains open. How do you use **Try-with-resources** to ensure all streams close automatically ? **
-	**NIO vs. IO**: You are building a chat server that needs to handle 5,000 concurrent connections. Why would you choose `java.nio` (Channels/Selectors) over standard `java.io` (Streams) ? **

### File handling
### Streams (byte/char)
### Buffered streams
## Locale / Internationalization in Java
-	** What is Locale in Java? **
-	** How will you use a specific Locale in Java? **

### Serialization / Deserialization
-	** What is serialization and deserialization in Java  ? ** 
-	** What are Serializable interface and transient variables in Java  ? ** 
-	** Can we serialize static variables in java? **
-	** What is serialization in java? **
-	** What is the main purpose of serialization in java? **
-	** What is Serialization and Deserialization conceptually? **
-	** What is the purpose of serialization? **
-	** What is Deserialization? **
-	** How do you fix your Serializable class, if it contains a member that is not serializable ? **
-	** What is serialization in Java ? **
-	** What is the difference between serialization and deserialization in Java ? **
-	** How to exclude a particular variable from serialization in a Java entity class ? **

### Serializable Interface & Object Serialization
-	** Explain about serializable interface in java? **
-	** How to make object serializable in java? **
-	** When we serialize an object does the serialization mechanism saves its references too? **
-	** What is the serialization? **

### serialVersionUID & Alternatives
-	** What is serial version UID and its importance in java? **
-	** What happens if we don't define serial version UID ? **
-	** What are alternatives to java serialization? **

### Transient, Static & Skipping Fields
-	** Why do we mark a data member transient? **
-	** Is it allowed to mark a method as transient? **
-	** How does marking a field as transient makes it possible to serialize an object? **
-	** If we don't want some of the fields not to serialize How to do that? **
-	** Can we serialize static variables in java? **

### Externalizable Interface
-	** What is Externalizable interface in Java? **
-	** What is the difference between Serializable and Externalizable interface? **
### NIO (channels, buffers, selectors)
### Memory-mapped files

## 1.10 Java 8+ Features
-	** What are the key Java features introduced in versions 7, 8, 9, 11, 17, 21, and 25 ? **
-	** What are all the features of Java 11  ? ** 
-	** Which is the latest version of Java  ? ** 
-	** What are the new features released in Java 8? **
-	** What are the main benefits of new features introduced in Java 8? **
-	** Why did Oracle release a new version of Java like Java 8? **
-	** How can we analyze the dependencies in Java classes and packages? **
-	** What are the new JVM arguments introduced by Java 8? **
-	** What are the popular annotations introduced in Java 8? **

### Lambda expressions
-	** What is a Lambda expression in Java 8? **
-	** What are the three main parts of a Lambda expression in Java? **
-	** What is the data type of a Lambda expression? **
-	** What is the type of a Lambda expression in Java 8? **
-	** What is the target type of a lambda expression ? **
-	** What is the meaning of following lambda expression? **
-	** What are the advantages of a lambda expression? **
-	** What are the main differences between Internal and External Iterator? **
-	** What are the main advantages of Internal Iterator over External Iterator in Java 8? **
-	** What are the applications in which we should use Internal Iteration? **
-	** What is the main disadvantage of Internal Iteration over External Iteration? **
-	** How does Internal Iteration work in Java 8? **

### Functional interfaces
-	** What is a Functional interface in Java 8? **
-	** What is a Single Abstract Method (SAM) interface in Java 8? **
-	** How can we define a Functional interface in Java 8? **
-	** Why do we need Functional interface in Java? **
-	** Is it mandatory to use @FunctionalInterface annotation to define a Functional interface in Java 8? **
-	** What are the main differences between an interface with default method and an abstract class in Java 8? **
-	** What are Functional Interfaces in Java 8 ? **
-	** What is the difference between a Functional Interface and a standard @Annotation , and what changed after Java 8 ? **
-	** What is the difference between Consumer, Predicate, Supplier, and Function in Java 8 ? **
-	** What is the comparison between common Predefined Functional Interfaces ? **
-	** What is the difference between Runnable and Comparator as Functional Interfaces ? **
-	** What is the difference between implementing an Interface in a Class versus using a Functional Interface with a Lambda ? **
-	** What are the code examples for reusing and organising Functional Interfaces ? **

### Streams API (map, filter, reduce)
-	** What is Stream API in Java  ? ** 
-	** What are all the methods available in Stream API in Java  ? ** 
-	** What is the reduce method in Stream API in Java  ? ** 
-	** What is the difference between Stream and Parallel Stream in Java  ? ** 
-	** How to find the sum of ages of employees where age is greater than 35 using Stream API in Java  ? ** 
-	** What are the differences between Collection and Stream API in Java 8? **
-	** What are the main uses of Stream API in Java 8? **
-	** What are the differences between Intermediate and Terminal Operations in Java 8 Streams? **
-	** What is a Spliterator in Java 8? **
-	** What are the differences between Iterator and Spliterator in Java 8? **
-	** What is Type Inference in Java 8? **
-	** Does Java 7 support Type Inference? **
-	** What is the new method family introduced in Java 8 for processing of Arrays on multi core machines? **
-	** What is the Stream API in Java 8 ? **
-	** What is the difference between Parallel and Sequential Streams in Java ? **
-	** What are the common methods used in the Java 8 Stream API ? **
-	** What is the difference between map and filter in the Stream API ? **
-	** What is the difference between map and filter in terms of Stream operations and efficiency ? **
-	** What is the difference between map and flatMap in the Stream API ? **
-	** What is Parallel Array Sorting ? **
-	** What are Type Annotations and Repeating Annotations in Java 8 ? **
-	** What is the improvement in HashMap before and after Java 8 ? **
-	** How do you remove duplicates from a List in Java 8 ? **
-	** What is the difference between the collect() method and the Collectors class in the Stream API ? **

### Optional
-	** What is Optional API in Java  ? ** 
-	** What are all the methods of Optional API in Java  ? ** 
-	** What is the difference between Optional.of() and Optional.ofNullable() in Java  ? ** 
-	** What is Optional in Java 8? **
-	** What are the uses of Optional? **
-	** Which method in Optional provides the fallback mechanism in case of null value? **

### Method references
### Default/static interface methods
-	** What are static blocks and static initializers in Java  ? ** 
-	** What are default vs static methods in Java interfaces  ? ** 
-	** Can we define a method inside an interface  ? ** 
-	** What is the use of default methods in Java interfaces for flexible implementation  ? ** 
-	** Can we provide implementation of a method in a Java Interface? **
-	** What is a Default Method in an Interface? **
-	** Why do we need Default method in a Java 8 Interface? **
-	** What is the purpose of a Static method in an Interface in Java 8? **
-	** Is it possible to define a static method in an Interface? **
-	** Can we access a static method of an interface by using reference of the interface? **
-	** Is it possible to have default method definition in an interface without marking it with default keyword? **
-	** Can we create a class that implements two Interfaces with default methods of same name and signature? **
-	** How Java 8 supports Multiple Inheritance? **
-	** In case we create a class that extends a base class and implements an interface If both base class and interface have a default method with same name and arguments, then which definition will be picked by JVM? **
-	** If we create same method and define it in a class , in its parent class and in an interface implemented by the class, then definition will be invoked if we access it using the reference of Interface and the object of class? **
-	** How does Java 8 solve Diamond problem of Multiple Inheritance? **
-	** What are the main differences between Collection and Stream API in Java 8? **  (also relevant to default‑method usage in collections)

### Date/Time API (java.time)
-	** How to avoid modifying a Date object in Java  ? ** 
-	** What are the core ideas behind the Date/Time API of Java 8? **
-	** What are the advantages of new Date and Time API in Java 8 over old Date API? **
-	** What are the differences between legacy and modern Java Date and Time APIs ? **
-	** What are the main differences between legacy Date/Time API in Java and Date/Time API of Java 8? **
-	** How can we get duration between two dates or time in Java 8? **
-	** How can we get current time by using Date/Time API of Java 8? **

### CompletableFuture

## 1.11 JVM Internals
### Class loading (bootstrap, extension, application)
-	** How does ClassLoader work in Java  ? ** 
-	** What are the different types of Classloaders in Java ? **
-	** Your client is complaining that your code is throwing NoClassDefFoundError or NoSuchMethodError, even though you are able to compile your code without error and method exists in your code.  What could be the reason behind this ? **

### Classloader hierarchy
### Bytecode
-	** What is bytecode in java  ? ** 
### JIT compiler
-	** What is JIT compiler in JVM  ? ** 
-	** What is JVM, JIT, JRE and their heap memory size in Java  ? ** 

## 1.12 Reflection & Annotations
### Reflection API
-	** What is Reflection in Java? **
-	** What are the uses of Reflection in Java? **
-	** How can we access private method of a class from outside the class? **
-	** How can we create an Object dynamically at Runtime in Java? **
-	** What is the System class in Java ? **
-	** What is the Reflection API in Java ? **

### Dynamic class loading
### Annotations (built-in, custom)
-	** How do you create a custom annotation in Java  ? ** 
### Annotation processing

## 1.13 Design Patterns
### Creational (Singleton, Factory, Builder)
-	** How to create a Singleton class in Java  ? ** 
### Structural (Adapter, Decorator, Proxy)
### Behavioral (Observer, Strategy, Command)
### Anti-patterns
### Scenarios
-	** What are all the design patterns used in your project  ? ** 

## 1.14 Garbage Collection (G1, ZGC, Shenandoah basics)
-	** What is Garbage Collection in Java? **
-	** Why Java provides Garbage Collector? **
-	** How does Garbage Collection work in Java? **
-	** When does an object become eligible for Garbage Collection in Java? **
-	** What kind of process is the Garbage collector thread? **
-	** Why do we use finalize() method in Java? **
-	** What are the different types of References in Java? **
-	** How can we reference an unreferenced object again? **
-	** What is the purpose of gc() in Java? **
-	** What is the purpose of the Runtime class? **
-	** How can we invoke an external process in Java? **
-	** What are the uses of Runtime class? **
### GC algorithms (mark-sweep, generational)
### Memory leaks

---

## 1.15 Java Programs
### Java Basic Programs
-	** Fibonacci Series in Java ? **
-	** Prime Number Program in Java & nth Prime Number Java ? **
-	** Palindrome Program in Java ? **
-	** Factorial Program in Java ? **
-	** Armstrong Number in Java ? **
-	** How to Generate Random Number in Java ? **
-	** How to Print Pattern in Java ? **
-	** How to Compare Two Objects in Java ? **
-	** How to Create Object in Java ? **
-	** How to Print ASCII Value in Java ? **
-	** Reverse a String / Number  ? **
-	** second / third largest from array using sort logic of temp variable  ? **
-	** pattern ? **

### Java collection Programs

## Scenario Based Questions
-	** how to identify how much concurrent users my spring boot can handle ? **

# 2. Spring Framework (Core)
-	** What is the difference between Spring and Spring Boot  ? ** 
-	** What is @ComponentScan  ? ** 
-	** What is Spring framework? **
-	** What are the benefits of Spring framework in software development? **
-	** What is Spring Boot? **
-	** What are the best practices of Spring Framework? **
-	** What are the key differences between a library and a framework ? **
-	** What is Inversion of Control ( IoC ) Container ? **
-	** What is Dispatcher servlet ? **
-	** What is the difference between Dependency Injection and Inversion of Control  ? **
-	** What is a Bean ? **
-	** What are the different bean scopes and their descriptions ? **
-	** What is the Bean lifecycle ? **
-	** How to manage a Bean ? **
-	** What is ApplicationContext ? **
-	** What is the difference between XML and Annotation configuration ? **
-	** What is the difference between XML and Bean configuration ? **
-	** How is Dependency management handled in a pom . xml ? **
-	** What is the use of Spring Profile ? **
-	** What is Swagger in Spring Boot ? **
-	** Why go for Maven over Ant ? **
-	** What is the Maven build lifecycle ? **
-	** What are the differences between Gradle and Maven ? **
-	** What are the key differences between Java , Kotlin , and Groovy ? **
-	** What are the differences between SLF4J , Log4j , and Logback ? **
-	** What is the difference between Embedded Tomcat and Standalone Tomcat ? **
-	** How to change the default embedded Tomcat server in Spring Boot ? **
-	** What is the difference between Lombok and Jackson ? **
-	** What are @Data , @Getter , and @Setter ? **

## 2.1 Spring Core
-	** What are the modules in Core Container of Spring framework? **
-	** What are the modules in Data Access/Integration layer of Spring framework? **
-	** What are the modules in Web layer of Spring framework? **
-	** What is the main use of Core Container module in Spring framework? **
-	** What is the purpose of Spring configuration file? **
-	** What are the main components of a typical Spring based application? **

### IoC (Inversion of Control)
-	** What problems are solved by the Spring Framework and what is the need for Spring Boot ? ** 
    -	https://www.youtube.com/watch?v=bNFoN956P2A&ab_channel=in28minutesCloud%2CDevOpsandMicroservices
	-	https://www.springboottutorial.com/spring-boot-vs-spring-mvc-vs-spring
-	** What is the purpose of Spring IoC container? **
-	** What is the main benefit of Inversion of Control (IOC) principle? **
-	** Does IOC containers support Eager Instantiation or Lazy loading of beans? **

### Dependency Injection (constructor, setter, field)
-	** What is dependency injection in Spring  ? ** 
-	** What are the different types of dependency injection in Spring  ? ** 
-	** What is @Autowired and why is it used for dependency injection in Spring  ? ** 
    -	It is used to let Spring's dependency injection container automatically resolve and inject collaborating beans into your bean, eliminating the need for manual object instantiation and promoting loose coupling.
-	** How is the @Required annotation used in a Spring Controller  ? ** 
-	** What is the @Required annotation in Spring and why was it deprecated  ? ** 
-	** What is @Autowired in Spring and how does it work for constructor, setter, and field injection  ? ** 
-	** Between two types of dependency injections, constructor injection and setter dependency injection, which one is better ? **
-	** What are the different types of events provided by Spring framework? **
-	** What is the difference between FileSystemResource and ClassPathResource? **
-	** What are the different roles in Dependency Injection (DI)? **
-	** What is the difference between Dependency Injection (DI) and Factory Pattern? **
-	** What is the difference between Setter and Constructor based Dependency Injection (DI) in Spring framework? **
-	** What are the drawbacks of Setter based Dependency Injection (DI) in Spring? **
-	** What are the design-patterns used in Spring framework? **

### Bean lifecycle
-	** What is the lifecycle of a Bean in Spring framework? **
-	** What are the two main groups of methods in a Bean's lifecycle? **
-	** Can we override main lifecycle methods of a Bean in Spring? **

### Bean scopes
-	** What is a Spring Bean? **
-	** What does the definition of a Spring Bean contain? **
-	** What are the different ways to provide configuration metadata to a Spring Container? **
-	** What are the different scopes of a Bean supported by Spring? **
-	** How will you define the scope of a bean in Spring? **
-	** Is it safe to assume that a Singleton bean is thread safe in Spring Framework? **
-	** Is the difference between a Singleton and Prototype bean in Spring? **
-	** How will you decide which scope- Prototype or Singleton to use for a bean in Spring? **
-	** What are Inner beans in Spring? **
-	** How can we inject a Java Collection in Spring framework? **
-	** What is Bean wiring in Spring? **
-	** What is Autowiring in Spring? **
-	** What are the different modes of Autowiring supported by Spring? **
-	** What are the cases in which Autowiring may not work in Spring framework? **
-	** Is it allowed to inject null or empty String values in Spring? **
-	** What is the difference between Java Singleton (JVM level) and Spring Singleton (Application level)  ? ** 
-	** What are all the bean scopes available in Spring  ? ** 

### ApplicationContext vs BeanFactory
-	** What is BeanFactory in Spring  ? ** 
-	** What is the use of BeanFactory in Spring framework? **
-	** Which is the most popular implementation of BeanFactory in Spring? **
-	** What is XMLBeanFactory in Spring framework? **
-	** What are the benefits of ApplicationContext in Spring? **
-	** How will you implement ApplicationContext in Spring framework? **
-	** Explain the difference between ApplicationContext and BeanFactory in Spring? **
-	** Between ApplicationContext and BeanFactory which one is preferable to use in Spring? **

### Configuration (XML, Java, annotations)
-	** What is the difference between XML and Annotation -	based configuration in Spring  ? ** 
-	** What is the difference between @Component , @Service , and @Controller annotations in Spring  ? ** 
-	** What is the difference between @Component and @Service in Spring  ? ** 
-	** What is the difference between @Configuration and @ConfigurationProperties in Spring Boot  ? ** 
-	** What is the @Import annotation in Spring and how does it differ from @ComponentScan  ? ** 
-	** How does @ComponentScan tell Spring which packages to scan for annotated classes to be managed as beans  ? ** 
-	** How do you load configuration classes from separate JARs using @Import annotation in Spring  ? ** 
-	** How do you use @Configuration and @PropertySource together to load external properties in Spring  ? ** 
-	** What is a Java-based Configuration in Spring? **
-	** What is the purpose of @Configuration annotation? **
-	** What is the difference between Full @Configuration and 'lite' @Beans mode? **
-	** In Spring framework, what is Annotation-based container configuration? **
-	** How will you switch on Annotation based wiring in Spring? **
-	** What is @Autowired annotation? **
-	** What is @Required annotation? **
-	** What are the two ways to enable RequiredAnnotationBeanPostProcessor in Spring? **
-	** What is @Qualifier annotation in Spring? **
-	** Name some popular Spring framework annotations that you use in your project? **

## 2.2 Spring Beans
### Bean definition
-	** What is the use of @Bean annotation in Spring  ? ** 
-	** What is the @Bean annotation in Spring and how does it differ from @Component  ? ** 
### Bean wiring / scopes
-	** How do you handle or avoid concurrent access issues in a Spring Boot controller  ? ** 
### Lazy vs eager init
-	** What is the @Lazy annotation and how does it affect the autowiring of a service in Spring  ? ** 
### Autowiring modes
-	** What is @Qualifier in Spring  ? ** 
-	** What is the difference between @Qualifier, @Autowired, and @Bean in Spring  ? ** 
### Profiles

## 2.3 Spring AOP
### Aspect-Oriented Programming
-	** What is Aspect-Oriented Programming (AOP) in Spring and how does it handle cross-cutting concerns  ? ** 
-	** What is the difference between @Qualifier and @Primary in Spring  ? ** 
-	** What is the difference between Spring AOP and Interceptors  ? ** 
-	** What kind of testing can be done in Spring Test Module? **
-	** What are the uses of AOP module in Spring framework? **
-	** What is Aspect Oriented Programming (AOP) **
-	** What is an Aspect in Spring? **
-	** In Spring AOP, what is the main difference between a Concern and a Cross cutting concern? **
-	** What is a Joinpoint in Spring AOP? **
-	** What is an Advice in Spring AOP? **
-	** What are the different types of Advice in Spring AOP? **
-	** What is a Pointcut in Spring AOP? **
-	** What is an Introduction in Spring AOP? **
-	** What is a Target object in Spring AOP? **
-	** What is a Proxy in Spring AOP? **
-	** What are the different types of AutoProxy creators in Spring? **
-	** What is Weaving in Spring AOP? **
-	** In Spring AOP, Weaving is done at compile time or run time? **
-	** What is XML Schema-based Aspect implementation? **
-	** What is Annotation-based aspect implementation in Spring AOP? **

### Join point, advice, pointcut
### Proxies (JDK vs CGLIB)
### Use cases (logging, transactions)

## 2.4 Spring Expression Language (SpEL)

---

# 3. Spring Boot

## 3.1 Core
-	** How are different Spring Boot setup types -	Eclipse started, spring.io & cmd cli  ? ** 
### Auto-configuration
-	** What are the components of Spring Boot  ? ** 
-	** What is @SpringBootApplication and which mandatory annotations does it include to start a Spring Boot application  ? ** 
    -	@SpringBootApplication is the primary mandatory annotation, which is a convenience annotation that combines @Configuration , @EnableAutoConfiguration , and @ComponentScan .
-	** What is auto-configuration and what is the use of @EnableAutoConfiguration in Spring Boot  ? ** 
	-	Auto-configuration is a mechanism where Spring Boot automatically configures beans based on the dependencies present in the classpath. @EnableAutoConfiguration (included in @SpringBootApplication ) tells Spring Boot to start adding beans based on classpath settings, other beans, and various property settings.
-	** How does @SpringBootApplication combine @EnableAutoConfiguration, @ComponentScan, and @SpringBootConfiguration  ? ** 
-	** What is the difference between Spring and Spring Boot  ? ** 
### DataSource Configuration
-	** How to configure connection to H2/SQL database  ? ** 
### Starter dependencies
-	** What are the common Spring Maven dependencies in a pom . xml and their uses  ? ** 
	-	spring -	boot -	starter -	web : used to build RESTful applications using Spring MVC and an embedded Tomcat server .
	-	spring -	boot -	starter -	data -	jpa : used to connect to SQL databases using Hibernate and Spring Data JPA .
	-	spring -	boot -	starter -	security : used to provide authentication and authorization for protecting application endpoints .
	-	spring -	boot -	starter -	test : used to provide testing libraries like JUnit , Hamcrest , and Mockito .
	-	spring -	boot -	starter -	actuator : used to provide built -	in endpoints for monitoring and managing the application in production .
	-	spring -	cloud -	starter -	config : used to connect the application to a centralized configuration server .
-	** How do you configure and send emails in a Spring Boot application using JavaMailSender  ? ** 
-	** How do you add dependencies using GUI after project creation in Spring Boot  ? ** 
### Spring Boot lifecycle
-	** What is the Spring Boot bootstrap process  ? ** 
-	** How does Spring Boot work internally  ? ** 
-	** How to convert a JAR application to a WAR in Spring Boot  ? ** 
-	** What is the Spring Boot application flow  ? ** 
### application.properties / YAML
-	** What is the difference between application.yml and application.properties files in Spring Boot  ? ** 
-	How to read value from .yml file in Spring ? 
-	** What are the most frequently used YAML properties in a Spring Boot application and how are they configured  ? ** 
-	** Can we have more than one property file in Spring Boot and how to use it  ? ** 
-	** How to connect to a database in Spring Boot  ? ** 
-	** Can a single Spring Boot project have multiple databases  ? ** 

### Profiles

## 3.2 Web
### REST APIs
-	** What is the difference between stateful and stateless APIs  ? ** 
-	** What is the difference between SOAP and REST in the context of web services  ? ** 
-	** What are the different types of HTTP methods and their use cases in Spring Boot  ? ** 
-	** How do you implement a simple User CRUD operation in Spring Boot  ? ** 
    -	using maven to update
	-	using gradle to update
-	** What is the process of creating an endpoint to fetch a value from a database in Spring Boot  ? ** 
-	** How do you use @RepositoryRestResource to expose REST endpoints without a controller class  ? ** 
-	** What is the difference between @RestController and @Controller  ? ** 
-	** How to pass multiple IDs to a GET API using path variable in Spring Boot  ? ** 
-	** What is the library used for @ResponseBody serialization in Spring Boot  ? ** 
-	** What is the use of ResponseEntity in Spring  ? ** 
-	** How do you use restTemplate.getForObject to consume a REST API in Spring Boot  ? ** 
-	** How do you enable HTTPS for localhost in a Spring Boot application  ? ** 
-	** How do you find which client machine is sending request to your servlet in Java ? **
-	** What is the difference between a Cookie and a Session object in Java ? **
-	** Which protocol does Browser and Servlet use to communicate with each other ? **
-	** What is HTTP Tunneling ? **
-	** Why do we use JSP instead of Servlet in Java ? **
-	** How do you implement Servlet Chaining in Java ? **
-	** What is the difference between sendRedirect and forward methods ? **
-	** What are the implicit objects in JSP ? **

### @RestController
-	** What is @RestController and how is it different from @Controller in Spring  ? ** 
-	** When should you return ResponseEntity vs a plain DTO as JSON from a Spring REST controller  ? ** 
-	** What is the difference between @Controller and @RestController in Spring  ? ** 
-	** What is the difference between @RestController and @Controller in Spring  ? ** 
### Request mapping
-	** What is @RequestMapping and how is it used in Spring MVC  ? ** 
-	** What is the difference between @PathVariable and @RequestParam in Spring  ? ** 
-	** What is the difference between @GetMapping, @PostMapping, @PutMapping, @PatchMapping and @DeleteMapping in Spring  ? ** 
-	** What is the difference between @PathVariable and @RequestParam in Spring  ? ** 
-	** What is the difference between @RequestAttribute, @RequestHeader, and @RequestBody in Spring MVC  ? ** 
-	** What are the different methods of request mapping in Spring Boot  ? ** 
-	** What are the types of produces in a Spring Boot REST API  ? ** 

### Validation
### Exception handling (@ControllerAdvice)
-	** How do you implement exception handling in Spring Boot  ? ** 
### JSON/XML handling
-	** What is Jackson JSON and how is it used in Spring  ? ** 
-	** How does MappingJackson2HttpMessageConverter work internally in Spring MVC  ? ** \
    -	It uses the Jackson ObjectMapper to serialize/deserialize Java objects to/from JSON by checking the 'Content-Type' and 'Accept' headers during the HttpMessageConverter read/write phase of the Request Response lifecycle.
-	** What is the difference between DOM and SAX parser in Java ? **

## 3.3 Embedded Servers
### Tomcat, Jetty, Undertow
-	** How do you replace the default embedded Tomcat server with Jetty or Undertow in a Spring Boot application  ? ** 

## 3.4 Actuator
### Health checks
-	** What is Spring Boot Actuator and how does it provide health checks and monitoring for an application  ? ** 
	-	[YT LINK](https://www.youtube.com/watch?v=KQv2gpF2Guo&list=PL6VhlFq6vOqpjIrQGTprBl7bwrpsvW1Oa&index=12&ab_channel=CoderUlagam)
-	** What is Spring Boot Actuator and what are some of its key endpoints like /health, /metrics, /env, /beans, and /loggers  ? ** 
-	** What is Spring Boot Actuator  ? ** 
-	** How do you find all bean classes in a Spring Boot project using Actuator  ? ** 

### Metrics
-	** How to find the response delay of a Spring Boot application  ? ** 
### Monitoring
-	** How do you implement Spring Boot Admin to monitor and manage Spring Boot applications  ? ** 
-	** How to find and handle duplicate API calls in Spring Boot  ? ** 
-	** Why is management.endpoints.web.exposure.include=* used and should it be used in production  ? ** 

---

# 4. Spring Data

## 4.1 JPA
-	** What is the use of Spring Data JPA  ? ** 
-	** Which DB is used in our application? and Why over the other  ? ** 
-	** What is the use of the spring-data-jpa dependency in a Spring Boot application  ? ** 
-	** When do we prefer a relational database ? **
-	** What are the use cases for MySQL , SQL Server , Oracle , and PostgreSQL ? **
-	** When do we prefer a NoSQL database ? **
-	** What are the use cases for MongoDB , Cassandra , Redis , and Neo4j ? **
-	** What is @Entity and its related annotations ? **
-	** What is @Table and its related annotations ? **
-	** What are DTO and POJO and their related annotations ? **
-	** What is Caching in JPA ? **
-	** What are the different levels of caching in JPA ? **
-	** What is the difference between Second -	Level Cache ( L2 ) and Query Cache ? **
-	** How to enable Second -	Level Caching in Spring Boot ? **
-	** What is the Query Cache in JPA / Hibernate ? **
-	** What are the different ways to query in JPA ? **
-	** What is a Native Query in JPA ? **
-	** What is the purpose of the mappedBy attribute in JPA ? **
-	** What is the difference between @Transient and volatile ? **
-	** What is the difference between @Ignore and @JsonIgnore ? **
-	** What is @Auditable ? **
-	** What is @MappedSuperclass ? **
-	** What is @Valid ? **
-	** What is a JPA CRUD operation ? **
-	** What are the different JPA mapping relationships ? **

### Hibernate & ORM Basics
-	** What is Hibernate framework? **
-	** What is an Object Relational Mapping (ORM)? **
-	** What is Object Relational Impedance Mismatch? **
-	** What are the main problems of Object Relational Impedance Mismatch? **
-	** What are the key characteristics of Hibernate? **
-	** How do you connect a Spring Boot application to a database using Spring Data JPA and application.yml configuration  ? ** 
-	** Can a class act as an entity without the @Id annotation in JPA / Hibernate  ? ** 
-	** How do you integrate Azure Key Vault with Spring Boot to securely inject DB credentials at runtime without hardcoding them in application.yml ? ** 
-	** What is One-to-One and Many-to-One mapping in JPA/Hibernate  ? ** 
-	** What are the benefits of JDBC abstraction layer module in Spring framework? **
-	** How does Spring support Object Relational Mapping (ORM) integration? **
-	** How does Spring framework makes JDBC coding easier for developers? **
-	** What is the purpose of JdbcTemplate? **
-	** What are the benefits of using Spring DAO? **
-	** What are the different ways to use Hibernate in Spring? **
-	** What types of Object Relational Mapping (ORM) are supported by Spring? **
-	** How will you integrate Spring and Hibernate by using HibernateDaoSupport? **
-	** What are the different types of the Transaction Management supported by Spring framework? **
-	** What are the benefits provided by Spring Framework's Transaction Management? **
-	** Given a choice between declarative and programmatic Transaction Management, which method will you choose? **
-	** What is an ORM framework ? **
-	** What is Hibernate framework ? **
-	** What are the features of Hibernate framework ? **
-	** What are the key benefits of Spring and Hibernate integration ? **
-	** How to integrate Spring and Hibernate step by step ? **
-	** What is the Hibernate Architecture ? **
-	** What is a Hibernate SessionFactory ? **
-	** What is Spring Data ? ** 
-	** What is the difference between Hibernate and JPA ? **

### Core Interfaces, Configuration & Mapping
-	** Can you tell us about the core interfaces of Hibernate framework? **
-	** What is the purpose of Configuration Interface in Hibernate? **
-	** How will you map the columns of a DB table to the properties of a Java class in Hibernate? **
-	** Does Hibernate make it mandatory for a mapping file to have .hbm.xml extension? **
-	** What are the different ORM levels in Hibernate? **
-	** What is ORM metadata? **
-	** What are the different ways to configure a Hibernate application? **
-	** How can we auto-generate primary key in Hibernate? **
-	** What is a derived property in Hibernate? **

### Entity lifecycle
### Sessions, States & Persistence context
-	** What are the steps for creating a SessionFactory in Hibernate? **
-	** What is the Transient state of an object in Hibernate? **
-	** What is the Detached state of an object in Hibernate? **
-	** In Hibernate, how can an object go in Detached state? **
-	** How will you re-attach an object in Detached state in Hibernate? **
-	** What is the Unit of Work design pattern? **
-	** What is the difference between session.save() and session.saveOrUpdate() methods in Hibernate? **
-	** What are the different options to retrieve an object from database in Hibernate? **
-	** What is the difference between load() and get() method in Hibernate? **
-	** When should we use get() method or load() method in Hibernate? **
-	** How can we check if an Object is in Persistent, Detached or Transient state in Hibernate? **
-	** What is the use of Dirty Checking in Hibernate? **

### Collections, Associations & Inheritance
-	** What are the different types of collections supported by Hibernate? **
-	** What are the different types of Association mappings supported by Hibernate? **
-	** What are the different types of Unidirectional Association mappings in Hibernate? **
-	** What is 'the inverse side of association' in a mapping? **
-	** What is the difference between a Set and a Bag in Hibernate? **
-	** What inheritance mapping strategies are supported by Hibernate? **
-	** What are the advantages of Hibernate framework over JDBC? **

### Hibernate internals
-	** What is Pessimistic Locking and Optimistic Locking in JPA, and how do JPA transaction locks, distributed locks, SchedulerLock, and Hibernate locks work  ? ** 
-	** What is the difference between Hibernate and JPA  ? ** 

### Lazy vs eager loading
-	** What is the difference between Eager and Lazy loading in JPA, and how does it relate to pagination and sorting  ? ** 
### Pagination
-	** How do you fetch records from a database using pagination and sorting in Spring Data JPA  ? ** 
### Indexing
-	** What is the difference between primary and secondary indexing in a database  ? ** 
### Cascading
### Fetch types

## 4.2 Repositories
### CrudRepository, JpaRepository
-	** How does data flow from Controller endpoint to Repository to Database  ? ** 
-	** Spring JPA vs Data JPA  ? ** 
-	** What are all the default methods available in JpaRepository in Spring Data JPA  ? ** 
-	** What is the difference between CrudRepository and JpaRepository, and what methods does each provide  ? ** 
-	** What is the @Repository annotation in Spring and what role does it play in the data access layer  ? ** 
### Query methods
### Query, Criteria & Stored Procedures
-	** What is Hibernate Query Language (HQL)? **
-	** What is Named Query in Hibernate? **
-	** How can we use Named Query in Hibernate? **
-	** What is Criteria API in Hibernate? **
-	** How will you order the results returned by a Criteria in Hibernate? **
-	** How does Example criterion work in Hibernate? **
-	** How will you call a stored procedure in Hibernate? **
-	** How can you see SQL code generated by Hibernate on console? **

### JPQL
-	** What is the difference between right join and left join in SQL  ? ** 
-	** Does LEFT JOIN allow null values from the right table in SQL  ? ** 
-	** What is the advantage of PreparedStatement over Statement class in Java ? **
-	** How can you prevent SQL Injection in Java Code ? **

### Native queries
-	** What is the difference between a native query and a normal (JPQL) query in Spring Data JPA  ? ** 

### Caching, Performance & Monitoring
-	** How can we get statistics of a SessionFactory in Hibernate? **
-	** What is the first level of cache in Hibernate? **
-	** What are the different second level caches available in Hibernate? **
-	** What are the options to disable second level cache in Hibernate? **
-	** What are the different strategies for cache mapping in Hibernate? **
-   ** What is Query Cache in Hibernate? **
-	** How can we monitor the performance of Hibernate in an application? **
-	** What are the different fetching strategies in Hibernate? **
-	** What is the difference between Immediate fetching and Lazy collection fetching? **
-	** What is 'Extra lazy fetching' in Hibernate? **
-	** How can we check is a collection is initialized or not under Lazy Initialization strategy? **
-	** Which techniques can be used to reduce Lock contention and improve performance? **

### Callbacks, Callback Interface & Design Patterns
-	** How can we use HibernateTemplate? **
-	** What is the purpose of Callback interface in Hibernate? **
-	** Why do we use POJO in Hibernate? **
-	** How can we mark an entity/collection as immutable in Hibernate? **
-	** What are the design-patterns used in Hibernate framework? **

## 4.3 Transactions
### Transaction & Locking
-	** How does Transaction management work in Hibernate? **
-	** Which is the default transaction factory in Hibernate? **
-	** What are the two locking strategies in Hibernate? **
-	** What is the use of version number in Hibernate? **
-	** What is the use of session.lock() method in Hibernate? **
### @Transactional
-	** What is @Transactional in Spring and how does it manage transactions with propagation and isolation levels  ? ** 
### Propagation
### Isolation levels
### ACID
-	** What is ACID properties in databases  ? ** 

## Database Scenarios
-	** How to overcome issues when fetching thousands of records ? **
-	** How to fetch the second largest salary ? **
-	** How to use JPA to store Y / N for a Boolean type instead of 0 / 1 ? ** 
-	** What is the difference between Database rollback and Java / JPA @Transactional annotation ? **
-	** How to update two tables at a time with a code example ? **
-	** How to handle commit and rollback when updating two tables at a time ? **
-	** What happens if @Transactional is not used but @OneToOne ( cascade = CascadeType . ALL ) is set and a failure occurs during child table insert ? **
-	** How to access multiple databases in a single Spring Boot microservice ? **

---

# 5. Spring MVC
-	** What is Spring MVC  ? ** 
-	** How does Web module work in Spring framework? **
-	** What are the main uses of Spring MVC module? **
-	** How does Spring MVC framework work? **
-	** What is DispatcherServlet? **
-	** Can we have more than one DispatcherServlet in Spring MVC? **
-	** What is WebApplicationContext in Spring MVC? **
-	** What is Controller in Spring MVC framework? **
-	** What is @RequestMapping annotation in Spring? **
-	** What are the main features of Spring MVC? **
-	** How can you upload a file in Spring MVC Application? **
-	** How will you handle exceptions in Spring MVC Framework? **
-	** What is the difference between DispatcherServlet and ContextLoaderListener in Spring? **

### Architecture
-	** What is @ComponentScan and how does it work with DispatcherServlet and ViewResolver in Spring MVC  ? ** 
	-	@ComponentScan detects beans, DispatcherServlet acts as the Front Controller, and ViewResolver maps logical view names to actual resources.
### DispatcherServlet
-	** What is the purpose of RequestDispatcher ? **
-	** What is DispatcherServlet in Spring MVC  ? ** 
-	** What is the difference between ApplicationContext and DispatcherServlet in Spring  ? ** 
### Request lifecycle
-	** How is a value converted from the UI to an object in the backend  ? ** 
-	** How can you upload a large file ? **
-	** What are the important status codes in HTTP ? **
-	** What is the meaning of Accept attribute in HTTP header ? **
-	**How will you implement Session tracking in JSP ? **
-	** How will you prevent creation of session in JSP ? **
-	** What is the main difference between a Session and Cookie in JSP ? **
### View resolvers
### Model, View, Controller
### Interceptors
-	** What is HandlerInterceptor in Spring MVC  ? ** 

---

# 6. Spring Security
### Authentication vs Authorization
-	** What is the difference between authentication and authorization in Spring Security  ? ** 
-	** How do microservices communicate with each other and how is security enabled between them  ? ** 
-	** How do you configure basic security in a Spring Boot application using Spring Security  ? ** 
-	** How do you secure a Spring Boot application, and which library is used for security ? ** 
-	** What are the different types of security mechanisms in Spring Boot such as Default Security, SAML, Kerberos, LDAP, OAuth2, JWT, OKTA, OpenID Connect (OIDC), and Single Sign-On (SSO), and when should each be used  ? ** 
-	** How do you protect database credentials (username/password) in a Spring Boot application using tools like Azure Key Vault or similar secret management solutions ? ** 
-	** What security mechanisms are used in your project  ? ** 

### Filters chain
-	** How will you secure Spring Boot endpoints  ? ** 
### JWT
### OAuth2
### CSRF, CORS
### Password encoding
-	** How to encrypt confidential user data before storing it to a repository in Spring Security  ? ** 

---

# 7. Spring Cloud / Microservices
-	** What is a microservice in Spring  ? ** 
-	** What are the benefits of microservices architecture  ? ** 
-	** What is the role of an architect in microservices architecture  ? ** 
-	** What is the advantage of microservices architecture over Service Oriented Architecture (SOA)  ? ** 
-	** How do you scale a Spring Boot project using Spring Cloud with service discovery, fault tolerance, and load balancing across multiple instances of core services  ? ** 
	-	Config Server 
		-	spring-cloud-config-server & spring-cloud-starter-netflix-eureka-client
		-	@EnableConfigServer, @EnableEurekaClient
		-	spring.cloud.config.server.git.uri=file:///D:/java/blog/blog-config
	-	Eureka Server 
		-	spring-cloud-starter-netflix-eureka-server
		-	@EnableEurekaServer
		-	management.endpoints.web.exposure.include=*
	-	Gateway 
		-	spring-cloud-starter-gateway & spring-cloud-starter-netflix-eureka-client
		-	@EnableEurekaClient
		-	spring:
			  cloud:
				gateway.discovery.locator:
				  enabled: true
				  lower-case-service-id: true
	-	Other services 
    	-	spring-boot-starter-web & spring-cloud-starter-config & spring-cloud-starter-netflix-eureka-client
    	-	@EnableEurekaClient
  		-	spring.config.import=optional:configserver:http://localhost:8888
	-	pom
		-	spring-boot-starter-parent -	default configuration & dependency management, default java version
		-	spring-boot-starter (including auto-configuration support, logging, and YAML) vs  spring-starter-web
		-	spring-starter-web vs spring-cloud
-	** Is it a good idea to provide a tailored service template for microservices development in an organization  ? ** 
-	** What are the disadvantages of using shared libraries approach to decompose a monolith application  ? ** 
-	** What are the characteristics of a good microservice  ? ** 
-	** What is bounded context in microservices  ? ** 
-	** Is it a good idea for microservices to share a common database  ? ** 
-	** Can we create microservices as state machines  ? ** 

### Service discovery (Eureka)
-	** What is Eureka and how does it facilitate service discovery in Spring Boot  ? ** 
-	** What are the standard annotations used in a Spring microservice project for web, config, Eureka, Zuul, and JPA  ? ** 

### API Gateway
-	** How do you communicate with the UI from a microservice  ? ** 
-	** Why are Zuul and Hystrix in maintenance mode and what are their modern replacements in Spring Cloud  ? ** 
-	** What is the difference between Zuul and Spring Cloud Gateway, and why should you prefer Spring Cloud Gateway  ? ** 
	-	[YT](https://youtu.be/puIJ1Mn9_LE)
### Load balancing
-	** What is a Load Balancer and how is it implemented in a Spring Cloud environment  ? ** 
-	** Why is @LoadBalanced annotation used with RestTemplate in Spring Cloud microservices  ? ** 

### Circuit breaker (Resilience4j)
-	**	What is Hystrix and how does it relate to fault tolerance in microservices  ? ** 
-	** What is the difference between Hystrix and Resilience4j as circuit breaker libraries, and why is Resilience4j preferred  ? ** 
-	** What is the difference between Hystrix and Resilience4j in Spring microservices  ? ** 

### Config server
-	** How do you fix the spring.cloud.config.server.git.uri issue where it always loads from a temp folder and does not refresh in Spring Cloud Config Server  ? ** 
-	** How do you use a service ID instead of an IP address in spring.config.import for Spring Cloud Config Server discovery  ? ** 
-	** What are the types of reading config server YAML from a client in Spring Cloud Config  ? ** 
-	** What are the YAML and other source options available for Spring Cloud Config Server  ? ** 
-	** How do you use {cipher<text>} in YAML to encrypt and decrypt config files in Spring Cloud Config Server  ? ** 

### Distributed tracing
### Feign client
-	** What is RestTemplate and how is it used to consume RESTful web services in Spring  ? ** 
-	** What is WebClient and how does it enable non-blocking communication in Spring  ? ** 
-	** What is the difference between RestTemplate and WebClient in Spring  ? ** 
-	** What are other ways to transfer data between two Spring Boot applications without using RestTemplate  ? ** 
### Scenarios
-	** How do you create and deploy a Spring Boot microservices application to the cloud  ? ** 

---

# 8. Messaging & Integration
-	** What are the points to remember during integration of microservices  ? ** 
-	** What is the preferred type of communication between microservices? Synchronous or asynchronous  ? ** 
-	** What is the difference between orchestration and choreography in microservices architecture  ? ** 
-	** What are the issues in using REST over HTTP for microservices  ? ** 

### Kafka
### RabbitMQ
-	** What is the use of RabbitMQ in a microservices application  ? ** 
-	** What is the Advanced Message Queuing Protocol (AMQP) and how does it work with exchanges, queues, and bindings in messaging systems like RabbitMQ  ? ** 
-	** What is the difference between JMS and RabbitMQ, and when should each be used in a messaging architecture  ? ** 
-	** What are the different types of RabbitMQ exchanges such as Default/Direct, Fanout, and Topic, and how does each route messages using routing keys  ? ** 
-	** What is the difference between an Exchange, Channel, and Queue in RabbitMQ and how do they work together in message flow  ? ** 
-	** What is the difference between a Producer and a Consumer in RabbitMQ, and how do they interact through exchanges and queues  ? ** 
-	** What is a Routing Key in RabbitMQ and how does it determine message routing from an exchange to a queue  ? ** 
-	** What is a Binding in RabbitMQ and how does it link an exchange to a queue for message routing, and what is the difference between binding and subscribing  ? ** 
-	** What is a Dead Letter Queue (DLQ) in RabbitMQ and how does it handle failed, rejected, or expired messages  ? ** 

### JMS
### Elasticsearch
-	** What is a Document and an Index in Elasticsearch and how are they related  ? ** 
-	** What is Aggregation in Elasticsearch and how does it work with count, terms, and bucket aggregations  ? ** 
-	** What is the difference between match query and filter in Elasticsearch, and when should each be used  ? ** 
-	** What is the difference between must and should clauses in an Elasticsearch bool query  ? ** 
-	** What is the difference between term and terms queries in Elasticsearch  ? ** 
-	** What is a nested query in Elasticsearch and when should it be used over a standard query  ? ** 

### Event-driven architecture	
-	** What is the difference between Spring Integration, Spring Event Listener, and REST Controller endpoint, and when should each be used  ? ** 
-	** What is the use of Elasticsearch in a microservices application  ? ** 

---

# 9. Testing
### JUnit
-	** What is the difference between @Mock and @InjectMocks in Mockito, and how do you configure them as a coding standard for unit testing  ? ** 
### Mockito
-	** What is the difference between Mockito.mock(), @Mock, and @MockBean in Mockito, and when should each be used  ? ** 
### Spring Boot Test
### Integration testing
### Testcontainers

---

# 10. Build & DevOps
### Git
-	** How can we see n most recent commits in GIT  ? ** 
-	** How can we know if a branch is already merged into master in GIT  ? ** 
-	** What is the purpose of git stash drop  ? ** 
-	** What is the HEAD in GIT  ? ** 
-	** What is the most popular branching strategy in GIT  ? ** 
-	** What is SubGit  ? ** 
-	** What is the use of git instaweb  ? ** 
-	** What are git hooks  ? ** 
-	** What is GIT  ? ** 
-	** What is a repository in GIT  ? ** 
-	** What are the main benefits of GIT  ? ** 
-	** What are the disadvantages of GIT  ? ** 
-	** What are the main differences between GIT and SVN  ? ** 
-	** How will you start GIT for your project  ? ** 
-	** What is git clone in GIT  ? ** 
-	** How will you create a repository in GIT  ? ** 
-	** What are the different ways to start work in GIT  ? ** 
-	** GIT is written in which language  ? ** 
-	** What does ‘git pull’ command in GIT do internally  ? ** 
-	** What does ‘git push’ command in GIT do internally  ? ** 
-	** What is git stash  ? ** 
-	** What is the meaning of ‘stage’ in GIT  ? ** 
-	**  What is the purpose of git config command  ? ** 
-	** How can we see the configuration settings of GIT installation  ? ** 
-	** How will you write a message with commit command in GIT  ? ** 
-	** What is stored inside a commit object in GIT  ? ** 
-	** How many heads can you create in a GIT repository  ? ** 
-	** Why do we create branches in GIT  ? ** 
-	** What are the different kinds of branches that can be created in GIT  ? ** 
-	** How will you create a new branch in GIT  ? ** 
-	** How will you add a new feature to the main branch  ? ** 
-	** What is a pull request in GIT  ? ** 
-	** What is merge conflict in GIT  ? ** 
-	** How can we resolve a merge conflict in GIT  ? ** 
-	** What command will you use to delete a branch  ? ** 
-	** What command will you use to delete a branch that has unmerged changes  ? ** 
-	** What is the alternative command to merging in GIT  ? ** 
-	** What is Rebasing in GIT  ? ** 
-	** What is the ‘Golden Rule of Rebasing’ in GIT  ? ** 
-	** Why do we use Interactive Rebasing in place of Auto Rebasing  ? ** 
-	** What is the command for Rebasing in Git  ? ** 
-	** What is the main difference between git clone and git remote  ? ** 
-	** What is GIT version control  ? ** 
-	** What GUI do you use for working on GIT  ? ** 
-	** What is the use of git diff command in GIT  ? ** 
-	** What is git rerere  ? ** 
-	** What are the three most popular version of git diff command  ? ** 
-	** What is the use of git status command  ? ** 
-	** What is the main difference between git diff and git status  ? ** 
-	** What is the use of git rm command in GIT  ? ** 
-	** What is the command to apply a stash  ? ** 
-	** Why do we use git log command  ? ** 
-	** Why do we need git add command in GIT  ? ** 
-	** Why do we use git reset command  ? ** 
-	** What does a commit object contain  ? ** 
-	** How can we convert git log messages to a different format  ? ** 
-	** What are the programming languages in which git hooks can be written  ? ** 
-	** What is a commit message in GIT  ? ** 
-	** How GIT protects the code in a repository  ? ** 
-	** How GIT provides flexibility in version control  ? ** 
-	** How can we change a commit message in GIT  ? ** 
-	** Why is it advisable to create an additional commit instead of amending an existing commit  ? ** 
-	** What is a bare repository in GIT  ? ** 
-	** How do we put a local repository on GitHub server  ? ** 
-	** How will you delete a branch in GIT  ? ** 
-	** How can we set up a Git repository to run code sanity checks and UAT tests just before a commit  ? ** 
-	** How can we revert a commit that was pushed earlier and is public now  ? ** 
-	** In GIT, how will you compress last n commits into a single commit  ? ** 
-	** How will you switch from one branch to a new branch in GIT  ? ** 
-	** How can we clean unwanted files from our working directory in GIT  ? ** 
-	** What is the purpose of git tag command  ? ** 
-	** What is cherry-pick in GIT  ? ** 
-	** What is shortlog in GIT  ? ** 
-	** How can you find the names of files that were changed in a specific commit  ? ** 
-	** How can we attach an automated script to run on the event of a new commit by push command  ? ** 
-	** What is the difference between pre-receive, update and post-receive hooks in GIT  ? ** 
-	** Do we have to store Scripts for GIT hooks within same repository  ? ** 
-	** How can we determine the commit that is the source of a bug in GIT  ? ** 
-	** How can we see differences between two commits in GIT  ? ** 
-	** What are the different ways to identify a commit in GIT  ? ** 
-	** When we run git branch <branchname>, how does GIT know the SHA-1 of the last commit  ? ** 
-	** What are the different types of Tags you can create in GIT  ? ** 
-	** How can we rename a remote repository  ? ** 
-	** Some people use git checkout and some use git co for checkout)  How is that possible  ? ** 
-	** How can we see the last commit on each of our branch in GIT  ? ** 
-	** Is origin a special branch in GIT  ? ** 
-	** How can we configure GIT to not ask for password every time  ? ** 
-	** What are the four major protocols used by GIT for data transfer  ? ** 
-	** What is GIT protocol  ? ** 
-	** How can we work on a project where we do not have push access  ? ** 
-	** What is git grep  ? ** 
-	** How can your reorder commits in GIT  ? ** 
-	** How will you split a commit into multiple commits  ? ** 
-	** What is filter-branch in GIT  ? ** 
-	** What are the three main trees maintained by GIT  ? ** 
-	** What are the three main steps of working GIT  ? ** 
-	** What are ours and theirs merge options in GIT  ? ** 
-	** How can we ignore merge conflicts due to Whitespace  ? ** 
-	** What is git blame  ? ** 
-	** What is a submodule in GIT  ? ** 

### Maven / Gradle
-	** What is the difference between JAR and WAR packaging in a Spring Boot application and when should each be used  ? ** 
-	** Why is version not mentioned for dependencies in pom.xml and from where is it taken  ? ** 
-	** How to check which Spring Boot version is used in a project via pom.xml or build.gradle  ? ** 
-	** How do you update Maven dependencies automatically or sequentially across services in a Spring Boot project  ? ** 
-	** What is Maven  ? ** 
-	** What are the main features of Maven  ? ** 
-	** What areas of a Project can you manage by using Maven  ? ** 
-	** What are the main advantages of Maven  ? ** 
-	** Why do we say “Maven uses convention over configuration”  ? ** 
-	** What are the responsibilities of a Build tool like Maven  ? ** 
-	** What are the differences between Ant and Maven  ? ** 
-	** What is MOJO in Maven  ? ** 
-	** What is a Repository in Maven  ? ** 
-	** What are the different types of repositories in Maven  ? ** 
-	** What is a local repository in Maven  ? ** 
-	** What is a central repository in Maven  ? ** 
-	** What is a Remote repository in Maven  ? ** 
-	** Why we should not store jars in CVS or any other version control system instead of Maven repository  ? ** 
-	** Can anyone upload JARS or artifacts to Central Repository  ? ** 
-	** What is a POM  ? ** 
-	** What is Super POM  ? ** 
-	** What are the main required elements in POM file  ? ** 
-	** What are the phases in Build lifecycle in Maven  ? ** 
-	** What command will you use to package your Maven project  ? ** 
-	** What is the format of fully qualified artifact name of a Maven project  ? ** 
-	** What is an Archetype in Maven  ? ** 
-	** What is the command in Maven to generate an Archetype  ? ** 
-	** What are the three main build lifecycles of Maven  ? ** 
-	** What are the main uses of a Maven plugin  ? ** 
-	** How will you find the version of a plugin being used  ? ** 
-	** What are the different types of profile in Maven? Where will you define these profiles  ? ** 
-	** What are the different setting files in Maven? Where will you find these files  ? ** 
-	** What are the main elements we can find in settings) xml  ? ** 
-	** How will you check the version of Maven in your system  ? ** 
-	** How will you verify if Maven is installed on Windows  ? ** 
-	** What is a Maven artifact  ? ** 
-	** What are the different dependency scopes in Maven  ? ** 
-	** How can we exclude a dependency in Maven  ? ** 
-	** How Maven searches for JAR corresponding to a dependency  ? ** 
-	** What is a transitive dependency in Maven  ? ** 
-	** What are Excluded dependencies in Maven  ? ** 
-	** What are Optional dependencies in Maven  ? ** 
-	** Where will you find the class files after compiling a Maven project successfully  ? ** 
-	** What are the default locations for source, test and build directories in Maven  ? ** 
-	** What is the result of jar:jar goal in Maven  ? ** 
-	** How can we get the debug or error messages from the execution of Maven  ? ** 
-	** What is the difference between a Release version and SNAPSHOT version in Maven  ? ** 
-	** How will you run test classes in Maven  ? ** 
-	** Sometimes Maven compiles the test classes but doesn't run them? What could be the reason for it  ? ** 
-	** How can we skip the running of tests in Maven  ? ** 
-	** Can we create our own directory structure for a project in Maven  ? ** 
-	** What are the differences between Gradle and Maven  ? ** 
-	** What is the difference between Inheritance and Multi-module in Maven  ? ** 
-	** What is Build portability in Maven  ? ** 

### CI/CD basics
-	** What is the end to end deployment process in a Java Spring Boot project  ? ** 
-	** What is SonarQube and how is it used for code quality analysis in Java projects  ? ** 
-	** What are the Twelve-Factor App principles and how do they guide building scalable, cloud-native microservices  ? ** 

### Docker basics
-	** What is Docker  ? ** 
-	** What is the difference between Docker image and Docker container  ? ** 
-	** How will you remove an image from Docker  ? ** 
-	** How is a Docker container different from a hypervisor  ? ** 
-	** Can we write compose file in json file instead of yaml  ? ** 
-	** Can we run multiple apps on one server with Docker  ? ** 
-	** What are the common use cases of Docker  ? ** 
-	** What are the main features of Docker-compose  ? ** 
-	** What is the most popular use of Docker  ? ** 
-	** What is the role of open source development in the popularity of Docker  ? ** 

### AWS 
-	** What do you know about AWS Region  ? ** 
-	** What are the important components of IAM  ? ** 
-	** What are the important points about AWS IAM  ? ** 
-	** What are the important features of Amazon S3  ? ** 
-	** What is the scale of durability in Amazon S3  ? ** 
-	** What are the Consistency levels supported by Amazon S3  ? ** 
-	** What are the different tiers in Amazon S3 storage  ? ** 
-	** How will you upload a file greater than 100 megabytes in Amazon S3  ? ** 
-	** What happens to an Object when we delete it from Amazon S3  ? ** 
-	** What is the use of Amazon Glacier  ? ** 
-	** Can we disable versioning on a version-enabled bucket in Amazon S3  ? ** 
-	** What are the use cases of Cross Region Replication Amazon S3  ? ** 
-	** Can we do Cross Region replication in Amazon S3 without enabling versioning on a bucket  ? ** 
-	** What are the different types of actions in Object Lifecycle Management in Amazon S3  ? ** 
-	** How do we get higher performance in our application by using Amazon CloudFront  ? ** 
-	** What is the mechanism behind Regional Edge Cache in Amazon CloudFront  ? ** 
-	** What are the benefits of Streaming content  ? ** 
-	** What is Lambda@Edge in AWS  ? ** 
-	** What are the different types of events triggered by Amazon CloudFront  ? ** 
-	** What is Geo Targeting in Amazon CloudFront  ? ** 
-	** What are the main features of Amazon CloudFront  ? ** 
-	** What are the security mechanisms available in Amazon S3? Cloud Computing  ? ** 
-	** What are the benefits of Cloud Computing  ? ** 
-	** What is On-demand computing in Cloud Computing  ? ** 
-	** What are the different layers of Cloud computing  ? ** 
-	** What resources are provided by Infrastructure as a Service (IAAS) provider  ? ** 
-	** What is the benefit of Platform as a Service  ? ** 
-	** What are the main advantages of PaaS  ? ** 
-	** What is the main disadvantage of PaaS  ? ** 
-	** What are the different deployment models in Cloud computing  ? ** 
-	** What is the difference between Scalability and Elasticity  ? ** 
-	** What is Software as a Service  ? ** 
-	** What are the different types of Datacenters in Cloud computing  ? ** 
-	** Explain the various modes of Software as a Service (SaaS) cloud environment  ? ** 
-	** What are the important things to care about in Security in a cloud environment  ? ** 
-	** Why do we use API in cloud computing environment  ? ** 
-	** What are the different areas of Security Management in cloud  ? ** 
-	** What are the main cost factors of cloud based data center  ? ** 
-	** How can we measure the cloud-based services  ? ** 
-	** How a traditional datacenter is different from a cloud environment  ? ** 
-	** How will you optimize availability of your application in a Cloud environment  ? ** 
-	** What are the requirements for implementing IaaS strategy in Cloud  ? ** 

### Kubernetes basics

---

# 11. Performance & Production
### Caching (Spring Cache, Redis)
-	** How is caching handled at the repository level in Spring applications  ? ** 
### Connection pooling
### Thread tuning
### JVM tuning
### Logging (SLF4J, Logback)
-	** How do you resolve the "SLF4J: Class path contains multiple SLF4J bindings" error in a Spring Boot application  ? ** 
-	** Which library is used for logging in Spring Boot  ? ** 
-	** Where is the log file generated for a Spring Boot project  ? ** 

---

# 12. Netty Internals
### Event-driven architecture
### Reactor pattern
### Channel, ChannelPipeline
### ChannelHandler (inbound vs outbound)
### EventLoop & EventLoopGroup
### ByteBuf (heap vs direct, pooling)
### Zero-copy (FileRegion, sendfile)
### Backpressure handling
### Thread model (boss vs worker)
### NIO vs Epoll/KQueue
### Memory management & leak detection

---

# 13. Bytecode Manipulation

## 13.1 JVM Bytecode Basics
### .class structure
### Stack-based execution model
### Bytecode instructions
### Class file format

## 13.2 ASM
### ClassReader, ClassWriter
### MethodVisitor, FieldVisitor
### Bytecode transformation
### Instrumentation

## 13.3 ByteBuddy
### Runtime class generation
### Method interception
### Proxies
### Agents

## 13.4 Java Agents
### premain / agentmain
### Instrumentation API
### Runtime weaving

---

# 14. Reactive Programming (WebFlux)

## 14.1 Reactive Fundamentals
### Reactive Streams spec
-	** What is Spring Reactive programming and how does WebFlux differ from traditional Spring MVC  ? ** 
### Publisher, Subscriber, Subscription
### Backpressure

## 14.2 Project Reactor
### Mono vs Flux
### Operators (map, flatMap, concatMap)
### Scheduler (boundedElastic, parallel)
### Cold vs hot streams

## 14.3 WebFlux Internals
### Netty integration
### Event loop model
### Functional vs annotation endpoints
-	** What is the Spring Flux or reactive way of application development in Spring Boot  ? ** 
### Thread model vs Spring MVC

## 14.4 Performance Concepts
### Blocking vs non-blocking cost
### Context switching
### Throughput vs latency

---

# 15. Advanced GC Tuning

## 15.1 GC Algorithms
### Serial, Parallel
### CMS
### G1
### ZGC, Shenandoah

## 15.2 Memory Layout
### Young, Old, Survivor spaces
### Region-based memory (G1)

## 15.3 Tuning Parameters
### Heap sizing (-Xms, -Xmx)
### GC logs analysis
### Pause time tuning
### Throughput tuning

## 15.4 GC Problems
### Stop-the-world pauses
### Promotion failure
### Memory fragmentation

## 15.5 Tools
### jstat, jmap, jvisualvm
### GC log analyzers

---

# 16. Database Internals

## 16.1 Storage Engine Basics
### Pages, blocks
### Row vs column storage

## 16.2 Indexing
### B-Tree, B+Tree
### Hash index
### Composite index
### Covering index

## 16.3 Query Execution
### Query planner
### Execution plan
### Cost-based optimization

## 16.4 Transactions
### ACID
### Isolation levels (read phenomena)
### MVCC

## 16.5 Locking
### Row-level vs table-level
### Deadlocks
### Optimistic vs pessimistic locking
-	** What is the difference between Pessimistic and Optimistic locking in JPA  ? ** 
-	** What is Pessimistic Locking and Optimistic Locking in JPA, and how do JPA transaction locks, distributed locks, SchedulerLock, and Hibernate locks work  ? ** 
-	** What is the difference between optimistic and pessimistic locking in Java  ? ** 

## 16.6 Replication & Scaling
### Master-slave
### Sharding
### Partitioning

## 16.7 Performance
### N+1 problem
### Index tuning
### Connection pooling